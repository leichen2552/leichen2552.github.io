<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://leichen2552.github.io/</id>
    <title>ChenLei&apos;s Blog</title>
    <updated>2020-02-09T04:16:24.752Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://leichen2552.github.io/"/>
    <link rel="self" href="https://leichen2552.github.io/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://leichen2552.github.io/images/avatar.png</logo>
    <icon>https://leichen2552.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, ChenLei&apos;s Blog</rights>
    <entry>
        <title type="html"><![CDATA[回顾（22）：对象的销毁]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-22dui-xiang-de-xiao-hui</id>
        <link href="https://leichen2552.github.io/post/hui-gu-22dui-xiang-de-xiao-hui">
        </link>
        <updated>2020-02-09T03:01:05.000Z</updated>
        <content type="html"><![CDATA[<h1 id="对象的销毁">对象的销毁</h1>
<ul>
<li>生活中的对象都是被初始化之后才上市的</li>
<li>生活中的对象被销毁前会做一些清理工作</li>
</ul>
<pre><code>问题：C++中如何清理需要销毁的对象？
</code></pre>
<h2 id="添加public的free函数">添加public的free函数</h2>
<ul>
<li>一般而言，需要销毁的对象都应该被清理</li>
<li>解决方案：
<ul>
<li>为每个类都提供一个public的free函数</li>
<li>对象不需要时立即调用free函数进行清理</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">class Test
{
    int* p;
public:
    Test() {p = new int}
    void free() {delete p;}
}
</code></pre>
<h3 id="存在的问题">存在的问题</h3>
<ul>
<li>free只是一个普通的函数，必须显示调用</li>
<li>对象销毁没有做处理，很可能造成资源泄露</li>
</ul>
<pre><code>想法：C++编译器能否自动调用某个特殊函数进行对象的清理？
</code></pre>
<h2 id="析构函数">析构函数</h2>
<ul>
<li>C++的类中可以定义个特殊的清理函数
<ul>
<li>这个特殊的清理函数叫做析构函数</li>
<li>析构函数的功能与构造函数相反</li>
</ul>
</li>
<li>定义：~ClassName()
<ul>
<li>析构函数没有参数也没有返回值类型，直接推出这个函数不能被<strong>重载</strong></li>
<li>析构函数在对象销毁时自动被调用</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">class Test
{
    int i;
public:
    Test(int v) 
    {
        i = v;
        printf(&quot;Test(): i = %d\n&quot;,i);
    }
    ~Test()
    {
        printf(&quot;~Test(): i = %d\n&quot;,i);
    }
}

int main()
{
    Test t(1);

    Test* p = new Test(2);

    delete p;

    return 0;
}
</code></pre>
<h2 id="修改之前的数组类">修改之前的数组类</h2>
<h3 id="inrarrayh">InrArray.h</h3>
<pre><code class="language-c++">#ifndef INTARRAY_H
#define INTARRAY_H

class IntArray{
private:
	int m_length;
	int* m_pointer;
public:
	IntArray(int len);
	IntArray(const IntArray&amp; ob);
	int length();
	bool get(int index, int&amp; value);
	bool set(int index, int value);	
	~IntArray();
};
</code></pre>
<p>#endif</p>
<h3 id="intarraycpp">IntArray.cpp</h3>
<pre><code class="language-c++">#include &quot;IntArray.h&quot;
#include &lt;iostream&gt;
using namespace std;//命名空间声明在此

IntArray::IntArray(int len)
{
	m_pointer = new int[len];
	
	for(int i = 0;i &lt; len; i++)
	{
		m_pointer[i] = 0;
	}
	
	m_length = len;	
}

IntArray::IntArray(const IntArray&amp; ob)
{
	m_length = ob.m_length;
	m_pointer = new int[ob.m_length];
	
	for(int i = 0;i &lt; ob.m_length;i++)
	{
		m_pointer[i] = ob.m_pointer[i]; 	
	}		
}

int IntArray::length()
{
	return m_length;
}

bool IntArray::get(int index, int&amp; value)
{
	bool ret = (0 &lt;= index)&amp;&amp;(index &lt; length());
	
	if(ret)
	{
		value = m_pointer[index];
	}
	
	return ret;
}

bool IntArray::set(int index, int value)
{
	bool ret = (0 &lt;= index)&amp;&amp;(index &lt; length());
	
	if(ret)
	{
 		m_pointer[index] = value;
	}
	
	return ret;
}

IntArray::~IntArray()
{
	cout &lt;&lt; &quot;~IntArray()&quot; &lt;&lt; endl;//在数组类实现细节的代码中加入输入输出，
                                  //必须要有命名空间的声明
	delete[] m_pointer;
}
</code></pre>
<h3 id="mainc">main.c</h3>
<pre><code class="language-c++">#include &lt;iostream&gt;
#include &quot;IntArray.h&quot;

using namespace std;

int main(int argc, char *argv[])
{
	IntArray a(5);
	
	for(int i=0; i&lt;a.length(); i++)
	{
		a.set(i,i+1);	
	}
	
	for(int i=0; i&lt;a.length(); i++)
	{
		int value = 0;
		if(a.get(i,value))
		{
			cout &lt;&lt; &quot;a[&quot;&lt;&lt; i&lt;&lt; &quot;]= &quot; &lt;&lt;value &lt;&lt; endl;
		}
	}
	
	IntArray b = a;
	
	for(int i=0; i&lt;b.length(); i++)
	{
		int value = 0;
		if(b.get(i,value))
		{
			cout &lt;&lt; &quot;b[&quot;&lt;&lt; i&lt;&lt; &quot;]= &quot; &lt;&lt;value &lt;&lt; endl;
		}
	}
	return 0;
}
</code></pre>
<ul>
<li>析构函数的定义准则
<ul>
<li>当类中自定义了构造函数并且构造函数中使用了系统资源（如：内存申请，文件打开，等），需要自定义析构函数</li>
</ul>
</li>
</ul>
<h1 id="小结">小结</h1>
<ul>
<li>析构函数是对象销毁时进行清理的特殊函数</li>
<li>析构函数在对象销毁时自动被调用</li>
<li>析构函数是对象释放系统资源的保障，意义非常重要，可以让我们编写的软件能更加稳定！</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（21）：对象构造顺序]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-21dui-xiang-gou-zao-shun-xu</id>
        <link href="https://leichen2552.github.io/post/hui-gu-21dui-xiang-gou-zao-shun-xu">
        </link>
        <updated>2020-02-09T02:21:14.000Z</updated>
        <content type="html"><![CDATA[<h1 id="对象的构造顺序">对象的构造顺序</h1>
<pre><code>C++中的类可以定义多个对象，那么对象构造的顺序是怎样的？
</code></pre>
<h2 id="对于局部对象">对于局部对象</h2>
<ul>
<li>当程序执行流到达对象的定义语句时进行构造</li>
</ul>
<pre><code class="language-c++">class Test
{
private:
    int mi;
public:
    Test()
    {
        mi = i;
        printf(&quot;Test(int i): %d\n&quot;, mi);
    }   
    Test()
    {
        mi = obj.mi;
        printf(&quot;Test(const Test&amp; obj): %d\n&quot;, mi);
    } 
};

int main
{
    int i = 0;
    Test a1 = i;

    while(i&lt;3)
    {
        Test a2 = ++i;
    }

    if(i &lt; 4)
    {
        Test a = a1;
    }
    else
    {
        Test a(100);
    }

    return 0;
}

</code></pre>
<h2 id="对于堆对象">对于堆对象</h2>
<ul>
<li>当程序执行流到达new语句时创建对象</li>
<li>使用new创建对象将自动触发构造函数的调用</li>
</ul>
<pre><code class="language-c++">int main
{
    int i = 0;
    Test* a1 = new Test(i);

    while(++i&lt;10)
    {
        if(i % 2)
            new Test(i);
    }

    if(i &lt; 4)
    {
        new Test(*a1);
    }
    else
    {
        new Test a(100);
    }

    return 0;
}
</code></pre>
<h2 id="对于全局对象">对于全局对象</h2>
<ul>
<li>全局对象的构造顺序是不确定的
<ul>
<li>全局对象的构造顺序是不确定的
<ul>
<li>全局对象的构造顺序是不确定的</li>
</ul>
</li>
</ul>
</li>
<li>不同的编译器使用不同的规则确定构造顺序</li>
</ul>
<h1 id="小结">小结</h1>
<ul>
<li>局部对象的构造顺序依赖于程序的执行流，goto语句会破坏程序的执行流</li>
<li>堆对象的构造顺序依赖于new的使用顺序</li>
<li>全局对象的构造顺序是不确定的，也是日常工程开发bug的主要来源之一</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（20）：初始化列表的使用]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-20chu-shi-hua-lie-biao-de-shi-yong</id>
        <link href="https://leichen2552.github.io/post/hui-gu-20chu-shi-hua-lie-biao-de-shi-yong">
        </link>
        <updated>2020-02-08T08:12:46.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#%E9%97%AE%E9%A2%98">问题</a></li>
<li><a href="#%E5%88%9D%E5%A7%8B%E5%8C%96%E5%88%97%E8%A1%A8">初始化列表</a>
<ul>
<li><a href="#%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9">注意事项</a></li>
<li><a href="#%E5%88%9D%E5%A7%8B%E5%8C%96%E5%92%8C%E8%B5%8B%E5%80%BC%E4%B8%8D%E5%90%8C">初始化和赋值不同</a></li>
</ul>
</li>
<li><a href="#%E5%B0%8F%E7%BB%93">小结</a></li>
</ul>
</p>
<h1 id="问题">问题</h1>
<pre><code>问题：类中能否定义const成员变量？
</code></pre>
<pre><code class="language-c++">#include &lt;iostream&gt;
using namespace std;

class Test
{
private:
	const int ci;
public:  
	Test()
	{
		ci = 10;
	}  
	int getI() 
	{
		return ci;
	}
};

int main(int argc, char *argv[])
{
	
	Test t;
    return 0;
}
//如果没有构造函数，编译器会报错，因为t中存在没有初始化的const成员变量
//如果有构造函数对ci进行初始化，编译器会报错，只读变量是不能出现在赋值符号的左边
</code></pre>
<h1 id="初始化列表">初始化列表</h1>
<ul>
<li>C++中提供了初始化列表对成员变量进行初始化</li>
<li>语法规则</li>
</ul>
<pre><code class="language-c++">className::className():m1(v1),m2(v1,v2),m3(v3)
{
    //some other initialize operation
}
</code></pre>
<pre><code class="language-c++">#include &lt;iostream&gt;
#include &lt;string&gt;
using namespace std;

class Test
{
private:
	const int ci;
public:  
	Test():ci(10)
	{

	}  
	int getI() 
	{
		return ci;
	}
};

int main(int argc, char *argv[])
{
	
	Test t;
	cout &lt;&lt; &quot;t.getI() = &quot;&lt;&lt; t.getI() &lt;&lt;endl;
    return 0;
}

//这个const变量是只读变量，依然是可以通过指针来修改值
Test():ci(10)
{
    int* p = const_cast&lt;int*&gt;(&amp;ci);
    *p = 5;
}  
</code></pre>
<h2 id="注意事项">注意事项</h2>
<ul>
<li>成员的初始化顺序与成员的声明顺序相同</li>
<li>成员的初始化顺序与初始化列表的位置无关</li>
<li>初始化列表先于构造函数的函数体执行</li>
</ul>
<pre><code class="language-c++">#include &lt;stdio.h&gt;

class Value
{
private:
    int mi;
public:
    Value(int i)
    {
        printf(&quot;i = %d\n&quot;, i);
        mi = i;
    }
    int getI()
    {
        return mi;
    }
};

class Test
{
private:
    Value m2;
    Value m3;
    Value m1;
public:
    Test() : m1(1), m2(2), m3(3)//成员的初始化顺序与成员的声明顺序相同,2,1,3
    {
        printf(&quot;Test::Test()\n&quot;);
    }
};
</code></pre>
<ul>
<li>类中的const成员会被分配空间</li>
<li>类中的const成员的本质是只读变量</li>
<li>类中的const成员只能在初始化列表中指定初始值</li>
</ul>
<pre><code>编译器无法得到const成员的初始值，因为无法进入符号表成为真正意义上的常量
</code></pre>
<h2 id="初始化和赋值不同">初始化和赋值不同</h2>
<ul>
<li>初始化：对正在创建的对象进行初值设置</li>
<li>赋值：对已经存在的对象进行值设置</li>
</ul>
<h1 id="小结">小结</h1>
<ul>
<li>类中可以使用初始化列表对成员进行初始化</li>
<li>初始化列表先于构造函数体执行</li>
<li>类中可以定义const成员变量</li>
<li>const成员变量必须在初始化列表中指定初值</li>
<li>const成员变量为只读变量，依然可以通过指针来修改</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（19）：对象的构造（下）]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-19dui-xiang-de-gou-zao-xia</id>
        <link href="https://leichen2552.github.io/post/hui-gu-19dui-xiang-de-gou-zao-xia">
        </link>
        <updated>2020-02-07T07:56:13.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#%E7%89%B9%E6%AE%8A%E7%9A%84%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0">特殊的构造函数</a>
<ul>
<li><a href="#%E6%97%A0%E5%8F%82%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0">无参构造函数</a></li>
<li><a href="#%E6%8B%B7%E8%B4%9D%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0">拷贝构造函数</a></li>
<li><a href="#%E6%8B%B7%E8%B4%9D%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E7%9A%84%E6%84%8F%E4%B9%89">拷贝构造函数的意义</a>
<ul>
<li><a href="#%E4%BB%80%E4%B9%88%E6%97%B6%E5%80%99%E9%9C%80%E8%A6%81%E6%B7%B1%E6%8B%B7%E8%B4%9D">什么时候需要深拷贝</a></li>
<li><a href="#%E6%95%B0%E7%BB%84%E7%B1%BB%E6%94%B9%E8%BF%9B">数组类改进</a>
<ul>
<li><a href="#intarrayh">IntArray.h</a></li>
<li><a href="#intarraycpp">IntArray.cpp</a></li>
<li><a href="#maincpp">main.cpp</a></li>
</ul>
</li>
</ul>
</li>
</ul>
</li>
<li><a href="#%E5%B0%8F%E7%BB%93">小结</a></li>
</ul>
</p>
<h1 id="特殊的构造函数">特殊的构造函数</h1>
<h2 id="无参构造函数">无参构造函数</h2>
<ul>
<li>没有参数的构造函数
<ul>
<li>当类中没有定义构造函数，编译器默认提供一个无参构造函数，并且其函数体为空</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">#include &lt;stdio.h&gt;
class Test
{
private:
    int i;
    int j;
public:
    int getI(){ return i;}
    int getJ(){ return j;}
    //编译器为什么能编译器通过。
    //我们没有定义任何的构造函数时，编译器就提供了默认的无参构造函数
    /*
    Test()
    {}
    */

    //类中并没有手工构造函数，就提供一个默认的拷贝构造函数，
    //但是如果只有一个手工拷贝构造函数，编译器就不会提供默认的无参构造函数
    //但是编译会出错，还需要一个手工无参构造函数
      /*
    Test(const Test&amp; t)
    {
        i = t.i;
        j = t.j;
    }
    */
};    

int main()
{
    Test t1;
    Test t2 = t1;

    cout &lt;&lt; &quot;t1.i = &quot; &lt;&lt; t1.i &lt;&lt; &quot;t1.j&quot; &lt;&lt; t1.j;
    cout &lt;&lt; &quot;t2.i = &quot; &lt;&lt; t2.i &lt;&lt; &quot;t2.j&quot; &lt;&lt; t2.j;

    return 0;
}
</code></pre>
<h2 id="拷贝构造函数">拷贝构造函数</h2>
<ul>
<li>参数为const class_name&amp; 的构造函数
<ul>
<li>当类中没有定义拷贝构造函数，编译器默认提供一个拷贝构造函数，简单的进行成员变量的值的赋值</li>
</ul>
</li>
</ul>
<h2 id="拷贝构造函数的意义">拷贝构造函数的意义</h2>
<ul>
<li>
<p>兼容C语言的初始化方式</p>
</li>
<li>
<p>初始化行为能够符合预期的逻辑</p>
</li>
<li>
<p>浅拷贝</p>
<ul>
<li>拷贝后对象的物理状态相同</li>
</ul>
</li>
<li>
<p>深拷贝</p>
<ul>
<li>拷贝后对象的逻辑状态相同</li>
</ul>
</li>
</ul>
<p><strong>编译器提供的拷贝构造函数只进行浅拷贝</strong></p>
<pre><code class="language-c++">#include &lt;iostream&gt;
#include &lt;string&gt;
using namespace std;
#include &lt;stdio.h&gt;
class Test
{
private:
    int i;
    int j;
    int* p; 
public:
    int getI(){ return i;}
    int getJ(){ return j;}
    int* getP(){return p;}
    Test(int v)
    {
        i = 1;
        j = 2;
        p = new int;

        *p = v;
    }
    Test(const Test&amp; t)
    {
        i = t.i;
        j = t.j;
        p = new int;

        *p = *t.p;
    }   
    void free()
    {
        delete p;
    }
};    //类的创建一定要加分号

int main()
{
    Test t1(3);
    Test t2 = t1;

    cout &lt;&lt; &quot;t1.i = &quot; &lt;&lt; t1.getI() &lt;&lt; &quot;,t1.j =&quot; &lt;&lt; t1.getJ() &lt;&lt; &quot;,t1.p = &quot;&lt;&lt; t1.getP() &lt;&lt; &quot;,*t1.p = &quot;&lt;&lt; *t1.getP() &lt;&lt; endl;
    cout &lt;&lt; &quot;t2.i = &quot; &lt;&lt; t2.getI() &lt;&lt; &quot;,t2.j =&quot; &lt;&lt; t2.getJ() &lt;&lt; &quot;,t2.p = &quot;&lt;&lt; t2.getP() &lt;&lt; &quot;,*t2.p = &quot;&lt;&lt; *t2.getP() &lt;&lt; endl;

    t1.free();
    t2.free();
   
    return 0;
}
</code></pre>
<p><strong>这两个指针指向了一个内存空间，并且被释放了两次，采用浅拷贝编译器会报错，如图所示</strong><br>
<img src="https://leichen2552.github.io//post-images/1581067898285.png" alt="" loading="lazy"></p>
<h3 id="什么时候需要深拷贝">什么时候需要深拷贝</h3>
<ul>
<li>对象中有成员指代了系统中的资源
<ul>
<li>成员指向了动态内存空间</li>
<li>成员打开了外存中的文件</li>
<li>成员使用了系统中的网络端口</li>
</ul>
</li>
</ul>
<h3 id="数组类改进">数组类改进</h3>
<p>因为上一节课的数组类中有对堆空间的申请，所以需要添加深拷贝构造函数</p>
<h4 id="intarrayh">IntArray.h</h4>
<pre><code class="language-c++">#ifndef INTARRAY_H
#define INTARRAY_H

class IntArray{
private:
	int m_length;
	int* m_pointer;
public:
	IntArray(int len);
	int length();
	bool get(int index, int&amp; value);
	bool set(int index, int value);	
    IntArray(const IntArray&amp; ob);
    void free();
};//类的创建一定要加分号！！！

#endif
</code></pre>
<h4 id="intarraycpp">IntArray.cpp</h4>
<pre><code class="language-c++">#include &quot;IntArray.h&quot;

IntArray::IntArray(int len)
{
	m_pointer = new int[len];
	
	for(int i = 0;i &lt; len; i++)
	{
		m_pointer[i] = 0;
	}
	
	m_length = len;	
}

IntArray::IntArray(const IntArray&amp; ob)
{
	m_length = ob.m_length;
	m_pointer = new int[ob.m_length];
	
	for(int i = 0;i &lt; ob.m_length;i++)
	{
		m_pointer[i] = ob.m_pointer[i]; 	
	}		
}

int IntArray::length()
{
	return m_length;
}

bool IntArray::get(int index, int&amp; value)
{
	bool ret = (0 &lt;= index)&amp;&amp;(index &lt; length());
	
	if(ret)
	{
		value = m_pointer[index];
	}
	
	return ret;
}

bool IntArray::set(int index, int value)
{
	bool ret = (0 &lt;= index)&amp;&amp;(index &lt; length());
	
	if(ret)
	{
 		m_pointer[index] = value;
	}
	
	return ret;
}

void IntArray::free()
{
	delete[] m_pointer;
}
</code></pre>
<h4 id="maincpp">main.cpp</h4>
<pre><code class="language-c++">#include &lt;iostream&gt;
#include &quot;IntArray.h&quot;

using namespace std;

int main(int argc, char *argv[])
{
	IntArray a(5);
	
	for(int i=0; i&lt;a.length(); i++)
	{
		a.set(i,i+1);	
	}
	
	for(int i=0; i&lt;a.length(); i++)
	{
		int value = 0;
		if(a.get(i,value))
		{
			cout &lt;&lt; &quot;a[&quot;&lt;&lt; i&lt;&lt; &quot;]= &quot; &lt;&lt;value &lt;&lt; endl;
		}
	}
	
	IntArray b = a;
	
	for(int i=0; i&lt;b.length(); i++)
	{
		int value = 0;
		if(b.get(i,value))
		{
			cout &lt;&lt; &quot;b[&quot;&lt;&lt; i&lt;&lt; &quot;]= &quot; &lt;&lt;value &lt;&lt; endl;
		}
	}
	a.free();
	b.free();
	return 0;
}
</code></pre>
<h1 id="小结">小结</h1>
<ul>
<li>C++编译器会默认提供构造函数</li>
<li>无参构造函数用于定义对象的默认初始状态，编译器发现没有任何的构造函数时，才会提供无参构造函数</li>
<li>拷贝构造函数在创建对象时拷贝对象的状态</li>
<li>对象的拷贝有深拷贝和浅拷贝两种方式
<ul>
<li>浅拷贝使得对象的物理状态相同</li>
<li>深拷贝使得对象的逻辑状态相同，如果使用了系统资源，就必须手动提供深拷贝函数</li>
</ul>
</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（18）：对象的构造（中）]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-18dui-xiang-de-gou-zao-zhong</id>
        <link href="https://leichen2552.github.io/post/hui-gu-18dui-xiang-de-gou-zao-zhong">
        </link>
        <updated>2020-02-07T06:10:50.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0">构造函数</a>
<ul>
<li><a href="#%E5%8F%8B%E6%83%85%E6%8F%90%E9%86%92">友情提醒</a></li>
<li><a href="#%E6%9E%84%E9%80%A0%E5%87%BD%E6%95%B0%E7%9A%84%E8%B0%83%E7%94%A8">构造函数的调用</a></li>
<li><a href="#%E5%B0%8F%E5%AE%9E%E4%BE%8B">小实例</a>
<ul>
<li><a href="#intarrayh">IntArray.h</a></li>
<li><a href="#intarraycpp">IntArray.cpp</a></li>
<li><a href="#maincpp">main.cpp</a></li>
</ul>
</li>
</ul>
</li>
<li><a href="#%E5%B0%8F%E7%BB%93">小结</a></li>
</ul>
</p>
<h1 id="构造函数">构造函数</h1>
<ul>
<li>带有参数的构造函数
<ul>
<li>构造函数可以根据需要定义参数</li>
<li>一个类中可以存在多个重载的构造函数</li>
<li>构造函数的重载遵循C++重载的规则</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">class Test
{
private:
    int i;
    int j;
public:
    Test() 
    {
    }
    Test(int v) 
    {//use v to initialize member
    }    
};
</code></pre>
<h2 id="友情提醒">友情提醒</h2>
<ul>
<li>对象定义和对象声明时不同的
<ul>
<li>对象定义：申请对象的空间并调用构造函数</li>
<li>对象声明：告诉编译器存在这样一个对象</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">Test t;//定义对象并调用构造函数

int main()
{
    extern Test t;//告诉编译器存在名为 t 的 Test 对象

    return 0;
}
</code></pre>
<p>构造函数的自动调用</p>
<pre><code class="language-c++">class Test
{
public:
    Test() 
    {
        printf(&quot;Test()&quot;);
    }
    Test(int v) 
    {//use v to initialize member
        printf(&quot;Test(int v)，v = %d\n&quot;,v);
    }    
};

int main()
{
    Test t;          //调用Test()
    Test t1(1);      //调用Test(int v)
    Test t2 = 1;     //调用Test(int v)

    t = t2;//赋值

    //啰嗦几句！
    int i = 1;//这个叫定义并且初始化

    int j;//这个初始化赋值是随机的
    j = 2;//这个叫做赋值

    int z(100);//这样子的初始化也合法
    printf(&quot;z = %d\n&quot;, z);

    return 0;
}    

</code></pre>
<p><strong>赋值和初始化是不相同！！！</strong></p>
<h2 id="构造函数的调用">构造函数的调用</h2>
<ul>
<li>一般情况下，构造函数在对象定义时被自动调用</li>
<li>一些特殊情况下，需要手工调用构造函数</li>
</ul>
<pre><code>如何创建一个对象数组？
</code></pre>
<pre><code class="language-c++">class Test
{
private:
    int m_value;
public:
    Test() 
    {
        printf(&quot;Test()&quot;);

        m_value = 0;
    }
    Test(int v) 
    {//use v to initialize member
        printf(&quot;Test(int v)，v = %d\n&quot;,v);

        m_value = v;
    }    
};

int main()
{
    Test ta[3] = {Test(), Test(1), Test(2)};//手工调用构造函数，
                                            //对数组中的每一个元素初始化

    for(int i = 0; i&lt;3; i++)
    {
        printf(&quot;ta[%d].getValue() = %d\n&quot;, i, ta[i].getValue());        
    }
    //从打印结果也能清晰的看出初始化时的调用函数情况

    Test t = Test(100);//手工调用构造函数进行初始化
    printf(&quot;t.getValue() = %d\n&quot;, t.getValue()); 
} 
</code></pre>
<h2 id="小实例">小实例</h2>
<ul>
<li>需求：开发一个数组类解决原生数组地安全性问题
<ul>
<li>提供函数获取数组长度</li>
<li>提供函数获取数组元素</li>
<li>提供函数设置数组元素</li>
</ul>
</li>
</ul>
<h3 id="intarrayh">IntArray.h</h3>
<pre><code class="language-c++">#ifndef INTARRAY_H
#define INTARRAY_H

class IntArray{
private:
	int m_length;
	int* m_pointer;
public:
	IntArray(int len);
	int length();
	bool get(int index, int&amp; value);
	bool set(int index, int value);	
    void free();
};//类的创建一定要加分号！！！

#endif
</code></pre>
<h3 id="intarraycpp">IntArray.cpp</h3>
<pre><code class="language-c++">#include &quot;IntArray.h&quot;

IntArray::IntArray(int len)
{
	m_pointer = new int[len];
	
	for(int i = 0;i &lt; len; i++)
	{
		m_pointer[i] = 0;
	}
	
	m_length = len;	
}

int IntArray::length()
{
	return m_length;
}

bool IntArray::get(int index, int&amp; value)
{
	bool ret = (0 &lt;= index)&amp;&amp;(index &lt; length());
	
	if(ret)
	{
		value = m_pointer[index];
	}
	
	return ret;
}

bool IntArray::set(int index, int value)
{
	bool ret = (0 &lt;= index)&amp;&amp;(index &lt; length());
	
	if(ret)
	{
 		m_pointer[index] = value;
	}
	
	return ret;
}

void IntArray::free()
{
	delete[] m_pointer;
}
</code></pre>
<h3 id="maincpp">main.cpp</h3>
<pre><code class="language-c++">#include &lt;iostream&gt;
#include &quot;IntArray.h&quot;

using namespace std;

int main(int argc, char *argv[])
{
	IntArray a(5);
	
	for(int i=0; i&lt;a.length(); i++)
	{
		a.set(i,i+1);	
	}
	
	for(int i=0; i&lt;a.length(); i++)
	{
		int value = 0;
		if(a.get(i,value))
		{
			cout &lt;&lt; &quot;a[&quot;&lt;&lt; i&lt;&lt; &quot;]= &quot; &lt;&lt;value &lt;&lt; endl;
		}
	}
	
	a.free();
	return 0;
}
</code></pre>
<p>其实这个类并不完善，以后会慢慢完善的</p>
<h1 id="小结">小结</h1>
<ul>
<li>构造哈数可以根据需要定义参数</li>
<li>构造函数之间可以存在重载关系</li>
<li>构造函数遵循C++中重载函数规则</li>
<li>对象定义时会触发构造函数的调用</li>
<li>在一些情况下可以手动调用构造函数</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（17）：对象的构造（上）]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-17dui-xiang-de-gou-zao-shang</id>
        <link href="https://leichen2552.github.io/post/hui-gu-17dui-xiang-de-gou-zao-shang">
        </link>
        <updated>2020-02-06T13:28:03.000Z</updated>
        <content type="html"><![CDATA[<h1 id="对象的初始化">对象的初始化</h1>
<pre><code>问题：对象中的成员变量初始值是多少？
</code></pre>
<p>试验一下就知道了！</p>
<pre><code class="language-c++">class Test
{
private:
    int i;
    int j;
public:
    void int getI() {return i;}
    void int getJ() {return j;}
}

//类得到的其实是数据类型

Test gt;//因为存储空间在全局数据区

int main()
{
    printf(&quot;gt.i = %d\n&quot;,gt.getI());
    printf(&quot;gt.j = %d\n&quot;,gt.getJ());

    Test t1;//因为存储空间在栈上面

    printf(&quot;t1.i = %d\n&quot;,t1.getI());
    printf(&quot;t1.j = %d\n&quot;,t1.getJ());

    Test* pt = new Test;

    printf(&quot;pt-&gt;i = %d\n&quot;,pt-&gt;getI());
    printf(&quot;pt-&gt;j = %d\n&quot;,pt-&gt;getJ());

    delete pt;
    return 0;
}
//打印结果
//i = 0
//j = 0
//i = 1236584
//j = 1256981
//i = 0，这个0只是一个巧合
//j = 0，这个0只是一个巧合
</code></pre>
<ul>
<li>从程序设计角度，对象只是变量，因此：
<ul>
<li>在栈上创建对象，成员变量初始为随机值</li>
<li>在堆上创建对象，成员变量初始为随机值</li>
<li>在静态存储区创建对象，成员变量初始值为0</li>
</ul>
</li>
</ul>
<h1 id="对象如何初始化">对象如何初始化</h1>
<ul>
<li>生活中的对象都是初始化后上市的</li>
<li>初始状态（出厂设置）是对象普遍存在的一个状态</li>
</ul>
<pre><code>问题：程序中如何对一个对象进行初始化
</code></pre>
<h2 id="一个public的initialize函数">一个public的initialize函数</h2>
<ul>
<li>一般而言，对象需要一个确定的初始状态</li>
<li>解决方案
<ul>
<li>再类中提供一个public的initialize函数</li>
<li>对象创建后理解调用initialize函数进行初始化</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">class Test
{
private:
    int i;
    int j;
public:
    void initialize() {i = 0; j = 0;}
    void int getI() {return i;}
    void int getJ() {return j;}
};
</code></pre>
<ul>
<li>存在的问题
<ul>
<li>initialize只是一个普通函数，必须显示调用</li>
<li>如果未调用initialize函数，运行结果是不确定的</li>
</ul>
</li>
</ul>
<p><strong>通过人手工来调用这个初始化函数太麻烦了！有没有那种刚创建完对象就自动来调用初始化的函数</strong></p>
<h2 id="构造函数">构造函数</h2>
<ul>
<li>C++中可以定义与类名相同的特殊成员函数
<ul>
<li>这种特殊的成员函数叫做构造函数
<ul>
<li>构造函数没有任何返回类型的声明</li>
<li>构造函数在对象定义时自动被调用</li>
</ul>
</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">class Test
{
private:
    int i;
    int j;
public:
    void int getI() {return i;}
    void int getJ() {return j;}
    Test() 
    {
        printf(&quot;Test() Enter\n&quot;);
        i = 0; 
        j = 0;
        printf(&quot;Test() Exit\n&quot;);
    }
};
</code></pre>
<h1 id="小结">小结</h1>
<ul>
<li>每个对象在使用之前都应该初始化</li>
<li>类的构造函数用于对象的初始化</li>
<li>构造函数与类同名并且没有返回值</li>
<li>构造函数在对象定义时自动被调用</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（16）：类的真正形态]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-16lei-de-zhen-zheng-xing-tai</id>
        <link href="https://leichen2552.github.io/post/hui-gu-16lei-de-zhen-zheng-xing-tai">
        </link>
        <updated>2020-02-06T09:17:37.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#%E7%B1%BB%E7%9A%84%E5%85%B3%E9%94%AE%E5%AD%97">类的关键字</a></li>
<li><a href="#%E5%B0%8F%E5%AE%9E%E4%BE%8B">小实例</a>
<ul>
<li><a href="#%E7%B1%BB%E7%9A%84%E7%9C%9F%E6%AD%A3%E5%BD%A2%E6%80%81">类的真正形态</a>
<ul>
<li><a href="#operatorh">Operator.h</a></li>
<li><a href="#operatorcpp">Operator.cpp</a></li>
<li><a href="#maincpp">main.cpp</a></li>
</ul>
</li>
</ul>
</li>
<li><a href="#%E5%B0%8F%E7%BB%93">小结</a></li>
</ul>
</p>
<h1 id="类的关键字">类的关键字</h1>
<p>经过不停地改进，结构体struct变得越来越不像它在C语言中的样子</p>
<ul>
<li>但是struct在C语言中已经有了自己的含义，必须继续兼容</li>
<li>在C++中提供了行的关键字class用于类定义</li>
<li>class和struct的用法是完全相同的</li>
</ul>
<pre><code>class和struct有什么区别呢？
</code></pre>
<ul>
<li>在struct定义类时，所有成员的默认访问级别是public</li>
<li>在class定义类时，所有成员的默认访问级别为private</li>
</ul>
<pre><code class="language-c++">struct A
{
    //default to public
    int i;
    //default to public
    int getI()
    {
        return i;
    }
};

class A
{
    //default to private
    int i;
    //default to private
    int getI()
    {
        return i;
    }
};
</code></pre>
<h1 id="小实例">小实例</h1>
<ul>
<li>需求：开发一个用于四则运算的类
<ul>
<li>提供setOperator函数设置运算类型</li>
<li>提供setParameter函数设置运算参数</li>
<li>提供result函数进行运算
<ul>
<li>其返回值表示运算的合法性</li>
<li>通过引用参数来返回结果</li>
</ul>
</li>
</ul>
</li>
</ul>
<h2 id="类的真正形态">类的真正形态</h2>
<ul>
<li>C++中的类支持声明和实现的分离</li>
<li>将类的实现和定义分开
<ul>
<li>.h头文件中只有类的声明
<ul>
<li>成员变量和成员函数的声明</li>
</ul>
</li>
<li>.cpp源文件中完成类的其他实现
<ul>
<li>成员函数的具体实现</li>
</ul>
</li>
</ul>
</li>
</ul>
<pre><code>使用方式与实现细节相分离
</code></pre>
<h3 id="operatorh">Operator.h</h3>
<pre><code class="language-c++">#ifndef OPEARTOR_H
#define OPERATOR_H

class Operator
{
private:
	char mOp;
	int mPa;
	int mPb;
	
public:
	bool setOperator(char mop);
  	bool setParameter(int mpa, int mpb);
    bool getresult(double&amp; r);		
	
};
</code></pre>
<h3 id="operatorcpp">Operator.cpp</h3>
<pre><code class="language-c++">#include &quot;Operator.h&quot;


bool Operator::setOperator(char mop)
{
	bool ret = true;
	
	if((mop == '/') || (mop == '+')||(mop == '-')||(mop == '*'))
	{
		ret = true;
		mOp = mop;
	}
	else
	{
		ret = false;
		mOp = '\0'		
	}
	
	return ret;
}

bool Operator::setParameter(int mpa, int mpb)
{
	mPa = mpa;
	mPb = mpb;
}

bool Operator::getresult(double&amp; r)
{
	bool ret = true;
	if( mOp == '/' &amp;&amp; mPb != 0 )
	{
		r = mPa/mPb;	
	}
	else
	{
		ret = false;
	}
	
	if(mOp == '+')
	{
		r = mPa+mPb;
	}
	if(mOp == '-')
	{
		r = mPa-mPb;
	}
	if(mOp == '*')
	{
		r = mPa*mPb;
	}
	
	return ret;
}	
</code></pre>
<h3 id="maincpp">main.cpp</h3>
<pre><code class="language-c++">#include &lt;iostream&gt;
#include &quot;Operator.h&quot;

using namespace std;

int main(int argc, char *argv[])
{
	Operator o;
	double r = 0;
	
	o.setOperator('/');
	o.setParameter(6, 3);
	if(o.getresult(r))
	{
		cout &lt;&lt; r &lt;&lt; endl;
	}
	
	return 0;
}
</code></pre>
<h1 id="小结">小结</h1>
<ul>
<li>C++引进新的关键字class用于定义类</li>
<li>struct和class的区别在于默认访问级别不同</li>
<li>C++中的类支持声明和实现的分离
<ul>
<li>在.h头文件中声明类的使用方式</li>
<li>在.cpp源文件中实现类的实现细节</li>
</ul>
</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（15）：类与封装的概念]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-15lei-yu-feng-zhuang-de-gai-nian</id>
        <link href="https://leichen2552.github.io/post/hui-gu-15lei-yu-feng-zhuang-de-gai-nian">
        </link>
        <updated>2020-02-06T04:56:03.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#%E7%B1%BB%E7%9A%84%E7%BB%84%E5%90%88">类的组合</a></li>
<li><a href="#%E7%B1%BB%E9%80%9A%E5%B8%B8%E5%88%86%E4%B8%BA%E4%BB%A5%E4%B8%8B%E4%B8%A4%E4%B8%AA%E9%83%A8%E5%88%86">类通常分为以下两个部分</a></li>
<li><a href="#%E7%B1%BB%E7%9A%84%E5%B0%81%E8%A3%85">类的封装</a>
<ul>
<li><a href="#%E5%B0%81%E8%A3%85%E7%9A%84%E5%9F%BA%E6%9C%AC%E6%A6%82%E5%BF%B5">封装的基本概念</a></li>
<li><a href="#c%E4%B8%AD%E7%B1%BB%E7%9A%84%E5%B0%81%E8%A3%85">C++中类的封装</a></li>
<li><a href="#%E7%B1%BB%E7%9A%84%E6%88%90%E5%91%98%E4%BD%9C%E7%94%A8%E5%9F%9F">类的成员作用域</a></li>
</ul>
</li>
<li><a href="#%E5%B0%8F%E7%BB%93">小结</a></li>
</ul>
<br>
类就是日常生活中分类的思想</p>
<h1 id="类的组合">类的组合</h1>
<ul>
<li>电脑这个类是由硬盘、内存、CPU、主板这些类组成的</li>
</ul>
<h1 id="类通常分为以下两个部分">类通常分为以下两个部分</h1>
<ul>
<li>类的实现细节</li>
<li>类的使用方式</li>
</ul>
<h1 id="类的封装">类的封装</h1>
<ul>
<li>
<p>当使用类时，不需要关心其实现细节</p>
</li>
<li>
<p>当创建类时，才需要考虑其内部实现细节</p>
</li>
<li>
<p>普通用户使用手机，只需要学习如何发短信、打电话、拍照等等</p>
</li>
<li>
<p>手机开发工程师，需要知道细节功能是如何实现的</p>
</li>
</ul>
<h2 id="封装的基本概念">封装的基本概念</h2>
<ul>
<li>根据经验，并不是类的每个属性都是对外公开的
<ul>
<li>如：女孩子不希望别人知道自己的体重和年龄</li>
<li>如：男孩子不希望别人知道自己的身高和收入</li>
</ul>
</li>
<li>而一些类的属性是对外公开的
<ul>
<li>如：人的姓名，学历，国籍，等</li>
</ul>
</li>
<li>必须在类的表示法中定义属性和行为的公开级别
<ul>
<li>类似文件系统中文件的权限</li>
</ul>
</li>
</ul>
<h2 id="c中类的封装">C++中类的封装</h2>
<ul>
<li>成员变量：C++中表示类属性的变量</li>
<li>成员函数：C++中用于表示类行为的函数</li>
<li>C++中可以给成员变量和成员函数定义访问级别
<ul>
<li>public：成员变量和成员函数可以在类的内部和外界访问和调用</li>
<li>private：成员变量和成员函数只能在类的内部被访问和调用</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">#include &lt;stdio.h&gt;

struct Biology {
    bool living;
};

struct Animal : Biology {
    bool movable;
    void findFood() { }
};

struct Plant : Biology {
    bool growable;
};

struct Beast : Animal {
    void sleep() { }
};

struct Human : Animal {
    void sleep() { }
    void work() { }
};

struct Girl : Human{
	private:
		int age;
		int weight;
	public:
		void print()
		{
			age = 22;
			weight = 48;
			
			printf(&quot;I'm girl, I'm %d years old\n&quot;,age);
			printf(&quot;My weight is %d kg\n&quot;,weight);
		}	
};

struct Boy : Human{
	private:
		int height;
		int salary;
	public:
		int age;
		int weight;
		void print()
		{
			height = 175;
			salary = 9000;
			age = 22;
			weight = 48;
			
			printf(&quot;I'm boy, my height is %d cm\n&quot;,height);
			printf(&quot;My salary is %d RMB\n&quot;,salary);
		}
};

int main(int argc, char *argv[]) {
	Girl g;//从C++语言的角度：创建了一个类的对象，
	Boy b;
	
	g.print();
	b.age = 19;
	b.weight = 120;
	
	
	b.print();
	return 0;
}
</code></pre>
<ul>
<li>成员函数可以访问类的的成员变量</li>
</ul>
<h2 id="类的成员作用域">类的成员作用域</h2>
<ul>
<li>类成员的作用域只在类的内部，外部是无法访问的</li>
<li>成员函数可以直接访问成员变量和调用成员函数</li>
<li>类的外部可以通过类变量访问public成员</li>
<li>类成员的作用域与访问级别没有任何关系
<ul>
<li>C++中用struct定义的类中所有成员默认为public</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">#include &lt;stdio.h&gt;

int i = 1;

struct Test
{
private:
    int i;

public:
    int j;
        
    int getI()
    {
        i = 3;
        
        return i;
    }
};

int main()
{
    int i = 2;
    
    Test test;
    
    test.j = 4;
    
    printf(&quot;i = %d\n&quot;, i);              //局部 i = 2;
    printf(&quot;::i = %d\n&quot;, ::i);          //全局 ::i = 1;
    // printf(&quot;test.i = %d\n&quot;, test.i);    //不能访问 Error
    printf(&quot;test.j = %d\n&quot;, test.j);    //public可以访问 test.j = 4
    printf(&quot;test.getI() = %d\n&quot;, test.getI());  // test.getI() = 3，
                         //类成员的作用域和类成员的访问级别是两个概念
    
    return 0;
}
</code></pre>
<h1 id="小结">小结</h1>
<ul>
<li>类通常可以分为使用方式和内部细节两部分</li>
<li>类的封装机制使得使用方式和内部细节相分离</li>
<li>C++中通过定义类成员的访问级别实现封装机制</li>
<li>public成员可以在类的背部和外界访问和调用</li>
<li>private成员只能在类的内部被访问和调用</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（14）：进阶面对对象（下）]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-14jin-jie-mian-dui-dui-xiang-xia</id>
        <link href="https://leichen2552.github.io/post/hui-gu-14jin-jie-mian-dui-dui-xiang-xia">
        </link>
        <updated>2020-02-05T08:20:26.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#%E7%B1%BB%E4%B9%8B%E9%97%B4%E7%9A%84%E5%85%B3%E7%B3%BB">类之间的关系</a></li>
<li><a href="#%E7%B1%BB%E7%9A%84%E8%A1%A8%E7%A4%BA%E6%B3%95">类的表示法</a>
<ul>
<li><a href="#%E5%A6%82%E4%BD%95%E8%AE%A9%E7%BC%96%E8%AF%91%E5%99%A8%E8%AF%BB%E6%87%82%E8%BF%99%E4%BA%9B%E7%B1%BB%E7%9A%84%E8%A1%A8%E7%A4%BA%E6%B3%95">如何让编译器读懂这些类的表示法</a></li>
</ul>
</li>
<li><a href="#%E5%B0%8F%E7%BB%93">小结</a></li>
</ul>
</p>
<h1 id="类之间的关系">类之间的关系</h1>
<ul>
<li>
<p>继承</p>
<ul>
<li>从已存在细分出来的类和原类之间有继承关系</li>
<li>继承的类（子类）拥有原类（父类）的所有属性和行为</li>
</ul>
</li>
<li>
<p>组合</p>
<ul>
<li>一些类的存在必须依赖于其他的类，这种关系叫组合</li>
<li>组合的类在某一个局部上有其他的类组成</li>
</ul>
</li>
<li>
<p>继承：惠普电脑、苹果电脑继承了电脑的所有属性</p>
</li>
<li>
<p>组合：电脑这个类需要硬盘、内存、主板、CPU这些类组合起来</p>
</li>
</ul>
<h1 id="类的表示法">类的表示法</h1>
<h2 id="如何让编译器读懂这些类的表示法">如何让编译器读懂这些类的表示法</h2>
<ul>
<li>类的表示法<br>
<img src="https://leichen2552.github.io//post-images/1580911863301.png" alt="" loading="lazy"></li>
<li>类的表示法-改进1<br>
<img src="https://leichen2552.github.io//post-images/1580911870890.png" alt="" loading="lazy"></li>
<li>类的表示法-改进2<br>
<img src="https://leichen2552.github.io//post-images/1580911881299.png" alt="" loading="lazy"></li>
<li>类的表示法-改进3<br>
<img src="https://leichen2552.github.io//post-images/1580911885439.png" alt="" loading="lazy"></li>
<li>类的表示法-改进4<br>
<img src="https://leichen2552.github.io//post-images/1580911900662.png" alt="" loading="lazy"></li>
<li>类的表示法-改进5<br>
<img src="https://leichen2552.github.io//post-images/1580911905917.png" alt="" loading="lazy"></li>
</ul>
<p>貌似大功告成，好像看着编译器可以识别了！</p>
<h1 id="小结">小结</h1>
<ul>
<li>类之间课已存在继承关系或者组合关系</li>
<li>继承关系中子类拥有父类的一切属性和行为</li>
<li>组合关系是类之间整体和部分的关系</li>
<li>类及类之间的关系可以有不同的表示法</li>
<li>编译器对类的表示法有具体的要求</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（13）：进阶面向对象（上）]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-13jin-jie-mian-xiang-dui-xiang-shang</id>
        <link href="https://leichen2552.github.io/post/hui-gu-13jin-jie-mian-xiang-dui-xiang-shang">
        </link>
        <updated>2020-02-04T07:34:35.000Z</updated>
        <content type="html"><![CDATA[<h1 id="面向对象基本概念">面向对象基本概念</h1>
<h2 id="面向对象的意义在于">面向对象的意义在于</h2>
<ul>
<li>将日常生活中习惯的思维引入程序设计中</li>
<li>将需求中的概念只管的映射到解决方案中</li>
<li>以模块为中心构建可复用的软件系统</li>
<li>提高软件产品的可维护性和可拓展性</li>
</ul>
<h2 id="类和对象是面向对象的两个基本概念">类和对象是面向对象的两个基本概念</h2>
<ul>
<li>类：指的是一类事物，是一个抽象的概念</li>
<li>对象：指的是属于某个类的具体实体</li>
<li>类是一种模型。这种模型课以创建出不同的对象实体</li>
<li>对象实体是类模型的一个具体事例</li>
</ul>
<p><code>一个类可以有很多对象，而一个对象必然属于某个类</code></p>
<p>对象就是一个具体实体，老虎分为华南虎、东北虎、孟加拉虎，这些是老虎的类别，成都动物园里的花花就是具体的对象</p>
<h2 id="类和对象的意义">类和对象的意义</h2>
<ul>
<li>类用于抽象的描述一类事物所特有的属性和行为
<ul>
<li>如：电脑拥有CPU，内存和硬盘，并且课以开机和运行程序</li>
</ul>
</li>
<li>对象是具体的事物，拥有所属类中描述一些属性和行为
<ul>
<li>如：每一只老虎都有各自的体重、食量和惰性</li>
</ul>
</li>
</ul>
<h3 id="一些有趣的问题">一些有趣的问题</h3>
<ul>
<li>类一定存在实际的对象吗？</li>
<li>类的对象数目是确定的吗？</li>
<li>类一定都来源于现实生活吗？</li>
<li>类都是独立的吗？类之间存在关系吗？</li>
<li>对象实例一定只属于一个类吗？</li>
<li>对象实例可能完全相同吗？</li>
</ul>
<h1 id="小结">小结</h1>
<ul>
<li>面向对象是当今软件开发中的重要方法</li>
<li>类和对象是面向对象理论中的基本概念</li>
<li>类和对象均来源于日常生活中</li>
<li>每个类可以有很多对象</li>
<li>每个对象必然属于某个类</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（12）：经典问题解析一]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-12jing-dian-wen-ti-jie-xi-yi</id>
        <link href="https://leichen2552.github.io/post/hui-gu-12jing-dian-wen-ti-jie-xi-yi">
        </link>
        <updated>2020-02-04T02:25:56.000Z</updated>
        <content type="html"><![CDATA[<h1 id="const">const</h1>
<ul>
<li>什么时候是只读变量？</li>
<li>什么时候是常量？</li>
</ul>
<h2 id="const常量的判别标准">const常量的判别标准</h2>
<ul>
<li>只有用字面量初始化的const常量才会进入符号表</li>
<li>使用其他变量初始化的const常量仍然是只读变量</li>
<li>被volatile修饰的const常量不会进入符号表，volatile修饰的就是易变的，是可能发生变化，可能会在外部被改变，所以通过volatile去访问这个变量就会去访问这个变量的内存。所以const修饰只是当前文件下的这个变量是只读属性</li>
</ul>
<p><code>在编译期间不能直接确定初始值的const标识符，都被作为只读变量</code></p>
<h2 id="关于const的提问">关于const的提问</h2>
<ul>
<li>const引用的类型与初始化变量的类型
<ul>
<li>相同：初始化变量成为只读变量</li>
<li>不同：生成一个新的只读变量</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">#include &lt;stdio.h&gt;

int main()
{
    const int x = 2;
    const int&amp; rx = x;
    int&amp; nrx = const_cast&lt;int&amp;&gt;(rx);

    nrx = 5;

    printf(&quot;x = %d\n&quot;,x);
    printf(&quot;rx = %d\n&quot;,rx);
    printf(&quot;nrx = %d\n&quot;,nrx);
    printf(&quot;&amp;x = %d\n&quot;,&amp;x);
    printf(&quot;&amp;rx = %d\n&quot;,&amp;rx);
    printf(&quot;&amp;nrx = %d\n&quot;,&amp;nrx);
    
    volatile const int y = 2;
    int* p = const_cast&lt;int*&gt;(&amp;y)

    *p = 6;

    printf(&quot;y = %d\n&quot;,y);
    printf(&quot;p = %p\n&quot;,p);

    const int z = y;

    p = const_cast&lt;int*&gt;(&amp;z);

    *p = 7;

    printf(&quot;z= %d\n&quot;, z);
    printf(&quot;p = %p\n&quot;, p);

    char c = 'c';
    char&amp; rc = c;
    const int&amp; trc = c;//这已经是一个新的只读变量了

    rc = 'a';

    printf(&quot;c= %d\n&quot;, c);       //a
    printf(&quot;rc = %p\n&quot;, rc);    //a

    printf(&quot;trc = %p\n&quot;, trc);  //c
    //const引用的类型与初始化变量的类型不同，会生成一个新的只读变量

    return 0;
}
</code></pre>
<h2 id="引用和指针有什么关系">引用和指针有什么关系</h2>
<ul>
<li>如何理解引用的本质就是指针常量
<ul>
<li>指针就是一个变量
<ul>
<li>值为一个内存地址，不需要初始化，可以保存不同的地址</li>
<li>通过指针访问对应内存地址中的值</li>
<li>指针可以被const修饰成为常量或者只读变量</li>
</ul>
</li>
<li>引用只是一个变量的新名字
<ul>
<li>对引用的操作（赋值，取地址等）都会传递到代表的变量上</li>
<li>const引用使其代表的变量具有只读属性</li>
<li>引用必须在定义时初始化，之后无法代表其他变量</li>
</ul>
</li>
</ul>
</li>
<li>从不同的角度来看
<ul>
<li>从使用C++语言角度的角度来看
<ul>
<li>引用于指针没有任何的关系</li>
<li>引用是变量的新名字，操作引用就是操作对应的变量</li>
</ul>
</li>
<li>从C++编译器的角度来看
<ul>
<li>为了支持新概念“引用”必须要一个有效的解决方案</li>
<li>在编译器内部，使用指针常量来实现引用</li>
<li>因此“引用”在定义时必须初始化</li>
</ul>
</li>
</ul>
</li>
<li>在工程项目开发中
<ul>
<li>当进行C++编程时，直接站在使用的角度看待引用，与指针毫无关系，引用就是变量的别名</li>
<li>当对C++代码进行调试分析时，一些特殊情况，可以考虑站在C++编译器的角度看待引用</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">#include &lt;stdio.h&gt;

int a = 1;

struct SV
{
    int&amp; x;
    int&amp; y;
    int&amp; z;
};

int main()
{
    int b = 2;
    int* pc = new int(3);
    SV sv = {a, b, *pc};
    int&amp; array[] = {a, b, *pc}; // &amp;array[1] - &amp;array[0] = ?  Expected ==&gt; 4
    /*
    C语言中的数组在内存中是依次顺序排放的。
    当然在C++中，也是需要兼容C语言中的这个特性，但是唯有引用数组就破坏了这个特性。
    比如上面的， &amp;array[1] - &amp;array[0]是不为4的，可以利用结构体SV查看x、y、z的地址。
    所以说，C++中不支持引用数组！！！
    */
    printf(&quot;&amp;sv.x = %p\n&quot;, &amp;sv.x);
    printf(&quot;&amp;sv.y = %p\n&quot;, &amp;sv.y);
    printf(&quot;&amp;sv.z = %p\n&quot;, &amp;sv.z);
    
    delete pc;
    
    return 0;
}
</code></pre>
<p><font  face = "Courier New" color = blue size = 5><strong>所以说，C++中不支持引用数组！</strong></font></p>
<h1 id="小结">小结</h1>
<ul>
<li>指针是一个变量</li>
<li>引用就是一个变量的新名字</li>
<li>const引用能够生成新的只读变量</li>
<li>在编译器内部使用指针常量实现引用，C++中为了兼容C语言数组特性——地址之差不是所期望的，所以只能抛弃引用数组</li>
<li>编译时不能直接确定初始值的const标识符都是只读变量</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（11）：新型的类型转换]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-11xin-xing-de-lei-xing-zhuan-huan</id>
        <link href="https://leichen2552.github.io/post/hui-gu-11xin-xing-de-lei-xing-zhuan-huan">
        </link>
        <updated>2020-02-03T08:08:18.000Z</updated>
        <content type="html"><![CDATA[<h1 id="强制类型转换">强制类型转换</h1>
<h2 id="c方式的强制类型转换">C方式的强制类型转换</h2>
<ul>
<li>( Type ) ( Expression )</li>
<li>Type( Expression )</li>
</ul>
<h2 id="c方式强制类型转换存在的问题">C方式强制类型转换存在的问题</h2>
<ul>
<li>过于粗暴
<ul>
<li>任意类型之间都可以进行转换，编译器很难判断其正确性</li>
</ul>
</li>
<li>难于定位
<ul>
<li>在源代码中无法快速定位所有使用强制类型转换的语句</li>
</ul>
</li>
</ul>
<pre><code>强制类型在实际工程中是很难完全避免的！！！
</code></pre>
<h2 id="新式类型转换">新式类型转换</h2>
<ul>
<li>static_cast</li>
<li>const_cast</li>
<li>dynamic_cast</li>
<li>reinterpret_cast</li>
</ul>
<h3 id="static_cast">static_cast</h3>
<ul>
<li>用于基本类型间的转换</li>
<li>不能用于基本类型指针间的转换</li>
<li>用于有继承关系类对象之间的转换和类指针之间的转换</li>
</ul>
<pre><code class="language-c++">void static_cast_demo()
{
    int i = 0x12345;
    char c = 'c';
    int* pi = &amp;i;
    char* pc = &amp;c;
    
    c = static_cast&lt;char&gt;(i);
    pc = static_cast&lt;char*&gt;(pi);//Error
}
</code></pre>
<h3 id="const_cast">const_cast</h3>
<ul>
<li>用于去除变量的只读属性</li>
<li>强制转换的目标类型必须是指针或引用</li>
</ul>
<pre><code class="language-c++">void const_cast_demo()
{
    const int&amp; j = 1;
    int&amp; k = const_cast&lt;int&amp;&gt;(j);
    
    const int x = 2;
    int&amp; y = const_cast&lt;int&amp;&gt;(x);
    
    int z = const_cast&lt;int&gt;(x);//Error
    
    k = 5;
    
    printf(&quot;k = %d\n&quot;, k);
    printf(&quot;j = %d\n&quot;, j);
    
    y = 8;
    
    printf(&quot;x = %d\n&quot;, x);
    printf(&quot;y = %d\n&quot;, y);
    printf(&quot;&amp;x = %p\n&quot;, &amp;x);
    printf(&quot;&amp;y = %p\n&quot;, &amp;y);
}
</code></pre>
<h3 id="reinterpret_cast">reinterpret_cast</h3>
<ul>
<li>用于指针类型间的强制转换</li>
<li>用于整数和指针类型间的强制转换</li>
</ul>
<pre><code class="language-c++">void reinterpret_cast_demo()
{
    int i = 0;
    char c = 'c';
    int* pi = &amp;i;
    char* pc = &amp;c;
    
    pc = reinterpret_cast&lt;char*&gt;(pi);
    pi = reinterpret_cast&lt;int*&gt;(pc);
    pi = reinterpret_cast&lt;int*&gt;(i);
    c = reinterpret_cast&lt;char&gt;(i); //Error
}

</code></pre>
<h3 id="dynamic_cast">dynamic_cast</h3>
<ul>
<li>用于有继承关系的类指针间的转换</li>
<li>用于有交叉关系的类指针间的转换</li>
<li>具有类型检查的功能</li>
<li>需要虚函数的支持</li>
<li>具有类型检查的功能，如果类型转换失败的话，会被转换成<strong>空指针</strong></li>
</ul>
<pre><code class="language-c++">void dynamic_cast_demo()
{
    int i = 0;
    int* pi = &amp;i;
    char* pc = dynamic_cast&lt;char*&gt;(pi);//Error
}

int main()
{
    static_cast_demo();
    const_cast_demo();
    reinterpret_cast_demo();
    dynamic_cast_demo();
    
    return 0;
}
</code></pre>
<h1 id="小结">小结</h1>
<ul>
<li>C方式的强制类型转换
<ul>
<li>过于粗暴</li>
<li>潜在的问题不易被发现</li>
<li>不易在代码中定位</li>
</ul>
</li>
<li>新式类型转换以C++关键字的方式出现
<ul>
<li>编译器能够帮助检查潜在的问题</li>
<li>非常方便的在代码中定位</li>
<li>支持动态类型识别（dynamic_cast）</li>
</ul>
</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（10）：C++中的新成员]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-10czhong-de-xin-cheng-yuan</id>
        <link href="https://leichen2552.github.io/post/hui-gu-10czhong-de-xin-cheng-yuan">
        </link>
        <updated>2020-02-03T01:01:48.000Z</updated>
        <content type="html"><![CDATA[<h1 id="动态内存分配">动态内存分配</h1>
<h2 id="c中的动态内存分配">C++中的动态内存分配</h2>
<ul>
<li>C++中通过new关键字进行动态内存申请</li>
<li>C++中的动态内存申请是基于类型进行的</li>
<li>delete关键字用于内存释放</li>
</ul>
<pre><code class="language-c++">//变量申请
Type* pointer = new Type;
//......
delete pointer;

//数组申请
Type* pointer = new Type[N];
//......
delete[] pointer;
</code></pre>
<pre><code>怎么申请的就怎么释放！有没有[]要注意！！！
</code></pre>
<pre><code class="language-c++">int p = new int;

p = new int[10];//这句动态内存申请，并不是表示p指向的是40个字节的一片内存空间
                //这样说的话，面试官会不给你过
                //因为动态内存分配时，分配的可能会比你申请的空间要大
                //应该这样说，p指向的内存空间至少40个字节
</code></pre>
<h2 id="new关键字与malloc函数的区别">new关键字与malloc函数的区别</h2>
<ul>
<li>new关键字是C++的一部分
<ul>
<li>malloc是由C库提供的函数</li>
</ul>
</li>
<li>new以具体类型为单位进行内存分配
<ul>
<li>malloc以字节为单位进行内存分配</li>
</ul>
</li>
<li>new在申请单个类型变量时可进行初始化
<ul>
<li>malloc不具备内存初始化的特性</li>
</ul>
</li>
</ul>
<h2 id="new关键字的初始化">new关键字的初始化</h2>
<pre><code class="language-c++">int* pi = new int(1);
float* pf = new float(2.0f);
char* pc = new char('c');
</code></pre>
<h1 id="c中的命名空间">C++中的命名空间</h1>
<h2 id="在c语言中只有一个全局作用域">在C语言中只有一个全局作用域</h2>
<ul>
<li>C语言所有的全局标识符共享同一个作用域</li>
<li>标识符之间可能发生冲突</li>
</ul>
<h2 id="c中提出了命名空间的概念">C++中提出了命名空间的概念</h2>
<ul>
<li>命名空间将全局作用域分成不同部分</li>
<li>不同命名空间中的标识符可以同名而不会发生冲突</li>
<li>命名空间可以相互嵌套</li>
<li>全局作用域也叫做默认命名空间</li>
</ul>
<h3 id="c命名空间的定义">C++命名空间的定义</h3>
<pre><code class="language-c++">namespace Name
{
    namespace Internal
    {
        /*  */
    }
    /**/
}
</code></pre>
<h3 id="c命名空间的使用">C++命名空间的使用</h3>
<ul>
<li>使用整个命名空间：</li>
</ul>
<pre><code class="language-c++">using namespace name;
</code></pre>
<ul>
<li>使用命名空间中的变量：</li>
</ul>
<pre><code class="language-c++">using name::varible;
</code></pre>
<ul>
<li>使用默认命名空间中的变量：</li>
</ul>
<pre><code class="language-c++">::varible
</code></pre>
<pre><code class="language-c++">#include &lt;stdio.h&gt;

namespace First
{
    int i = 0;
}

namespace Second
{
    int i =1;

    namespace Internal
    {
        struct P
        {
            int x;
            int y;
        };
    }
}

int main()
{
    using namespace First;
    using Second::Internal::P;

    printf(&quot;First::i = %d\n&quot;, i);
    printf(&quot;Second::i = %d\n&quot;, Second::i);

    P p = {2, 3};

    printf(&quot;p.x = %d\n&quot;, p.x);
    printf(&quot;p.y = %d\n&quot;, p.y);

    return 0;
}
</code></pre>
<h1 id="小结">小结</h1>
<ul>
<li>C++中内置的动态内存分配的专用关键字</li>
<li>C++中的动态内存分配可以同时进行初始化</li>
<li>C++中的动态内存分配时基于类型进行的</li>
<li>C++中命名空间概念用于解决名称冲突问题</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（9）：函数重载分析（下）]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-9han-shu-chong-zai-fen-xi-xia</id>
        <link href="https://leichen2552.github.io/post/hui-gu-9han-shu-chong-zai-fen-xi-xia">
        </link>
        <updated>2020-02-02T08:19:39.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#%E9%87%8D%E8%BD%BD%E4%B8%8E%E6%8C%87%E9%92%88">重载与指针</a>
<ul>
<li><a href="#%E5%87%BD%E6%95%B0%E9%87%8D%E8%BD%BD%E9%81%87%E4%B8%8A%E5%87%BD%E6%95%B0%E6%8C%87%E9%92%88">函数重载遇上函数指针</a></li>
<li><a href="#c%E5%92%8Cc%E7%9B%B8%E4%BA%92%E8%B0%83%E7%94%A8">C++和C相互调用</a>
<ul>
<li><a href="#%E6%B3%A8%E6%84%8F%E4%BA%8B%E9%A1%B9">注意事项</a></li>
</ul>
</li>
<li><a href="#%E5%B0%8F%E7%BB%93">小结</a></li>
</ul>
</li>
<li><a href="#%E9%97%AE%E9%A2%98">问题</a></li>
</ul>
</p>
<h1 id="重载与指针">重载与指针</h1>
<ul>
<li>下面的函数指针将保存哪个函数的地址？</li>
</ul>
<pre><code class="language-c++">int func(int x)
{
    return x;
}

int func(int a, int b)
{
    return a +b;
}

int func(const char* s)
{
    return strlen(s);
}

typedef int(*PFUNC)(int a);//函数指针类型为int
int c = 0;
PFUNC p = func;
c = p(1); 
</code></pre>
<h2 id="函数重载遇上函数指针">函数重载遇上函数指针</h2>
<ul>
<li>将重载函数名赋值给函数指针时
<ol>
<li>根据重载规则挑选与函数指针参数列表一致的候选者</li>
<li>严格匹配候选者的函数类型与函数指针的函数类型</li>
</ol>
</li>
</ul>
<pre><code>这也体现出C++是强类型的语言
</code></pre>
<ul>
<li>注意
<ul>
<li>函数重载必然发生在同一个作用域</li>
<li>编译器需要用参数列表或函数类型进行函数选择</li>
<li>无法直接通过函数名得到重载函数的入口地址</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">int func(int a, int b, int c)
{
    return a * b * c;
}

int func(int a, int b)
{
    return a + b;
}

int main()
{
    printf(&quot;%p\n&quot;,(int(*)(int, int))add);
    printf(&quot;%p\n&quot;,(int(*)(int, int, int))add);

    //下面这两句就饿没有指定函数指针的函数类型和参数列表的类型，所以肯定会报错
    printf(&quot;%p\n&quot;,add);
    printf(&quot;%p\n&quot;,add);

    return 0;
}
//打印出两个函数地址不同
</code></pre>
<h2 id="c和c相互调用">C++和C相互调用</h2>
<ul>
<li>实际工程中C++和C代码相互调用时不可避免的</li>
<li>C++编译器能够兼容C语言的编译方式</li>
<li>C++编译器会优先使用C++的编译的方式</li>
<li>extern关键字能强制让C++编译器进行C方式的编译</li>
</ul>
<pre><code class="language-c++">extern &quot;C&quot;
{
    //do C-style compilation here
}
</code></pre>
<ul>
<li>__cplusplus是C++内置的标准宏定义</li>
<li>__cplusplus的意义
<ul>
<li>确保C代码以统一的C方式被编译成目标文件</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">#ifdef __cplusplus
extern &quot;C&quot;{
#endif    

//C code

#ifdef __cplusplus
}
#endif
</code></pre>
<h3 id="注意事项">注意事项</h3>
<ul>
<li>C++编译器不能以C的方式编译重载函数</li>
<li>编译方式决定函数名被编译后的目标名
<ul>
<li>C++编译方式将函数名和参数列表编译成目标名</li>
<li>C编译方式只将函数名作为目标名进行编译</li>
</ul>
</li>
</ul>
<pre><code class="language-c++">//1
int func(int a, int b, int c)
{
    return a * b * c;
}

int func(int a, int b)
{
    return a + b;
}

//2
extern &quot;C&quot;{
    int func(int a, int b, int c)
    {
        return a * b * c;
    }

    int func(int a, int b)
    {
        return a + b;
    }
}
</code></pre>
<pre><code>    利用这句g++ -c test.cpp -o test.oo将上面两部分代码进行编译后
    用 nm 命令查看test.oo中的符号表，nm test.oo
    结果第一部分代码能编译通过，二部分代码编译报错
    所以C++编译方式将函数名和参数列表编译成目标名，C编译方式只将函数名作为目标名进行编译
</code></pre>
<h2 id="小结">小结</h2>
<ul>
<li>函数重载是C++对C的一个重要升级</li>
<li>函数重载通过函数参数列表区分不同的同名函数</li>
<li>extern关键字能够实现C和C++的相互调用</li>
<li>编译方式决定符号表中的函数名的最终目标名</li>
</ul>
<h1 id="问题">问题</h1>
<ul>
<li>C中如何调用C++函数？</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（8）：函数重载分析（上）]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-8han-shu-chong-zai-fen-xi</id>
        <link href="https://leichen2552.github.io/post/hui-gu-8han-shu-chong-zai-fen-xi">
        </link>
        <updated>2020-02-02T02:56:41.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#%E8%87%AA%E7%84%B6%E8%AF%AD%E8%A8%80%E4%B8%AD%E7%9A%84%E4%B8%8A%E4%B8%8B%E6%96%87">自然语言中的上下文</a>
<ul>
<li><a href="#%E6%B4%97%E5%AD%97%E7%9A%84%E5%90%AB%E4%B9%89">“洗”字的含义</a></li>
<li><a href="#%E7%BB%93%E8%AE%BA">结论</a></li>
</ul>
</li>
<li><a href="#%E9%87%8D%E8%BD%BDoverload">重载（overload）</a>
<ul>
<li><a href="#c%E4%B8%AD%E7%9A%84%E5%87%BD%E6%95%B0%E9%87%8D%E8%BD%BD">C++中的函数重载</a>
<ul>
<li><a href="#%E5%87%BD%E6%95%B0%E9%87%8D%E8%BD%BD">函数重载</a></li>
<li><a href="#%E5%87%BD%E6%95%B0%E9%87%8D%E8%BD%BD%E6%9D%A1%E4%BB%B6">函数重载条件</a></li>
<li><a href="#%E7%BC%96%E8%AF%91%E5%99%A8%E8%B0%83%E7%94%A8%E9%87%8D%E8%BD%BD%E5%87%BD%E6%95%B0%E7%9A%84%E5%87%86%E5%88%99">编译器调用重载函数的准则</a></li>
<li><a href="#%E5%87%BD%E6%95%B0%E9%87%8D%E8%BD%BD%E6%9C%AC%E8%B4%A8">函数重载本质</a></li>
<li><a href="#%E5%B0%8F%E7%BB%93">小结</a></li>
</ul>
</li>
</ul>
</li>
</ul>
</p>
<h1 id="自然语言中的上下文">自然语言中的上下文</h1>
<h2 id="洗字的含义">“洗”字的含义</h2>
<pre><code>洗衣服、洗脑、洗车、洗脸、洗马桶
</code></pre>
<h2 id="结论">结论</h2>
<ul>
<li>能和“洗”字搭配的词汇有很多</li>
<li>“洗”字和不同的词汇搭配有不同的含义</li>
</ul>
<h1 id="重载overload">重载（overload）</h1>
<ul>
<li>同一个标识符在不同的上下文有不同的意义</li>
<li>如：<br>
“洗”字和不同的词汇搭配有不同的含义： 洗衣服、洗脑、洗车、洗脸、洗马桶。。。<br>
“play”和不同的单词搭配后有不同的含义：play chess, play piano, play basketball, ...<br>
思考：</li>
</ul>
<pre><code>重载在自然语言中随处可见的，那在程序设计中是否也有重载呢？    
</code></pre>
<h2 id="c中的函数重载">C++中的函数重载</h2>
<h3 id="函数重载">函数重载</h3>
<ul>
<li>用同一个函数名定义不同的函数</li>
<li>当函数名和不同参数搭配时函数的含义不同</li>
<li>其实不止在C++中，在java、D语言、C#中，重载都是这么定义的</li>
</ul>
<pre><code class="language-c++">int func(int x)
{
    return x;
}

int func(int a, int b)
{
    return a + b;
}

int func(const char* s)
{
    return strlen(s);
}
</code></pre>
<h3 id="函数重载条件">函数重载条件</h3>
<ul>
<li>参数个数不同</li>
<li>参数类型不同</li>
<li>参数顺序不同</li>
</ul>
<pre><code class="language-c++">int func(int a, const char* s)
{
    return a;
}

int func(const char* s, int a)
{
    return strlen(s);
}
//以上两个函数可以构成重载
</code></pre>
<p>问题：</p>
<pre><code>当函数默认参数遇上函数重载会发生什么？
</code></pre>
<pre><code class="language-c++">int func(int a, int b, int c = 0)
{
    return a * b *c
}

int func(int a, int b)
{
    return a + b;
}

int main()
{
    int c = func(1, 2);// which one?
    return 0;
}
//编译器编译出错，就是编译器抱怨了，因为编译器不知道该去匹配哪个函数，出现了二义性！！！
//C++中有很多特性都是冲突的，所以我们要知道如何去避开这些特性冲突
</code></pre>
<h3 id="编译器调用重载函数的准则">编译器调用重载函数的准则</h3>
<ul>
<li>将所有同名函数作为候选者</li>
<li>尝试寻找可行的候选函数
<ol>
<li>精确匹配实参</li>
<li>通过默认参数能够匹配实参</li>
<li>通过默认类型转换匹配实参</li>
</ol>
</li>
<li>匹配失败
<ol>
<li>最终寻找到的候选函数不唯一，则出现二义性，编译失败</li>
<li>无法匹配所有候选者，函数未定义，编译失败</li>
</ol>
</li>
</ul>
<h3 id="函数重载本质">函数重载本质</h3>
<ul>
<li>
<p>重载函数在本质上是相互独立的不同函数</p>
</li>
<li>
<p>重载函数的函数类型不同</p>
</li>
<li>
<p>函数返回值不能作为函数重载的依据</p>
<p><code>函数重载是由函数名和参数列表决定的，跟函数返回值没有半毛钱关系!</code></p>
</li>
</ul>
<pre><code class="language-c++">int func(int a, int b, int c)
{
    return a * b * c;
}

int func(int a, int b)
{
    return a + b;
}

int main()
{
    printf(&quot;%p\n&quot;,(int(*)(int, int))add);
    printf(&quot;%p\n&quot;,(int(*)(int, int, int))add);

    return 0;
}
//打印出两个函数地址不同
</code></pre>
<h3 id="小结">小结</h3>
<ul>
<li>函数重载是C++中引入的概念</li>
<li>函数重载用于模拟自然语言中的词汇搭配</li>
<li>函数重载使得C++具有更加丰富的语义表达能力</li>
<li>函数重载的本质为相互独立的不同函数</li>
<li>C++中通过函数名和函数参数确定究竟调用那一个函数</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（7）：函数参数拓展]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-7han-shu-can-shu-tuo-zhan</id>
        <link href="https://leichen2552.github.io/post/hui-gu-7han-shu-can-shu-tuo-zhan">
        </link>
        <updated>2020-02-01T02:15:32.000Z</updated>
        <content type="html"><![CDATA[<h1 id="函数参数的默认值">函数参数的默认值</h1>
<ul>
<li>C++中可以在函数声明时为参数提供一个默认值</li>
<li>当函数调用时没有提供参数的值，则使用默认值</li>
</ul>
<pre><code class="language-c++">int mul(int x = 0);//提供一个参数默认值

int main()
{
    printf(&quot;%d\n&quot;, mul());  //mul(0)

    return 0;
}

int mul(int x)
{
    return x*x;
}
</code></pre>
<p>参数默认值必须在函数声明中指定，那么问题来了<br>
问题：</p>
<pre><code>    函数定义是否可以出现函数参数的默认值？
    当函数声明和定义中的参数默认值不同会发生什么？
</code></pre>
<pre><code class="language-c++">int mul(int x = 0);
//...
int mul(int x = 5)
{
    return x*x;
}      
</code></pre>
<p><code>函数参数默认值只能在声明时设置</code></p>
<h1 id="函数默认参数的规则">函数默认参数的规则</h1>
<ul>
<li>参数默认值必须从右向左提供</li>
<li>函数调用时使用了默认值，则后续参数必须使用默认值</li>
</ul>
<pre><code class="language-c++">int add(int x, int y = 1, int z = 2)
{
    return x + y+ z;
}

add(0);         //x = 0
add(2, 3);      //x = 2, y = 3
add(3, 2, 1);   //x = 3, y = 2, z = 1
</code></pre>
<p>C++是强类型语言</p>
<h1 id="函数的占位参数">函数的占位参数</h1>
<ul>
<li>占位参数只有参数类型声明，而没有参数名声明</li>
<li>一般情况下，在函数体内部无法使用占位参数</li>
</ul>
<pre><code class="language-c++">int func(int x, int)
{
    return x;
}

//  ...

func(1, 2);
</code></pre>
<h2 id="函数占位参数的意义">函数占位参数的意义</h2>
<ul>
<li>占位参数与默认参数结合起来使用</li>
<li>兼容C语言程序中可能出现的不规范写法</li>
</ul>
<p>举个例子：</p>
<pre><code class="language-c">#include &lt;stdio.h&gt;

void func()
{

}

int main()
{
    func();
    func(1, 2);

    return 0;
}
//这部分代码在C语言里是可以编译通过的
</code></pre>
<p>但是移植到C++环境里面，是编译不通过的，但是C++为了兼容C语言</p>
<pre><code class="language-c++">#include &lt;stdio.h&gt;

void func(int = 0, int = 0)
{

}

int main()
{
    func();
    func(1, 2);

    return 0;
}
</code></pre>
<h1 id="小结">小结</h1>
<ul>
<li>C++中支持函数参数的默认值</li>
<li>如果函数调用时没有提供参数，则使用默认值</li>
<li>参数的默认值必须从右向左提供</li>
<li>函数调用使用了默认值，则后续参数必须使用默认值</li>
<li>C++中支持占位参数，用于兼容C语言中的不规范写法</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（6）：内联函数分析]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-6nei-lian-han-shu-fen-xi</id>
        <link href="https://leichen2552.github.io/post/hui-gu-6nei-lian-han-shu-fen-xi">
        </link>
        <updated>2020-01-31T02:11:49.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#%E5%B8%B8%E9%87%8F%E4%B8%8E%E5%AE%8F%E5%9B%9E%E9%A1%BE">常量与宏回顾</a></li>
<li><a href="#%E9%97%AE%E9%A2%98">问题：</a>
<ul>
<li><a href="#%E5%86%85%E8%81%94%E5%87%BD%E6%95%B0">内联函数</a></li>
<li><a href="#%E5%86%85%E8%81%94%E5%87%BD%E6%95%B0%E7%89%B9%E6%80%A7">内联函数特性</a></li>
<li><a href="#%E5%86%85%E8%81%94%E5%87%BD%E6%95%B0%E5%88%9D%E6%8E%A2">内联函数初探</a></li>
<li><a href="#%E5%86%85%E8%81%94%E8%AF%B7%E6%B1%82">内联请求</a>
<ul>
<li><a href="#c%E4%B8%ADinline%E5%86%85%E8%81%94%E7%BC%96%E8%AF%91%E7%9A%84%E9%99%90%E5%88%B6">C++中inline内联编译的限制</a></li>
</ul>
</li>
</ul>
</li>
</ul>
</p>
<h1 id="常量与宏回顾">常量与宏回顾</h1>
<ul>
<li>C++中的const常量可以代替宏常数定义，如：<br>
const int A = 3; <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mo>←</mo><mo>→</mo></mrow><annotation encoding="application/x-tex">\leftarrow\rightarrow</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.36687em;vertical-align:0em;"></span><span class="mrel">←</span></span><span class="base"><span class="strut" style="height:0.36687em;vertical-align:0em;"></span><span class="mrel">→</span></span></span></span>#define A 3</li>
</ul>
<h1 id="问题">问题：</h1>
<pre><code>C++中是否有解决方案代替宏代码片段呢？
</code></pre>
<h2 id="内联函数">内联函数</h2>
<ul>
<li>宏代码块使用会有副作用的</li>
<li>C++中推荐使用内联函数代替宏代码片段</li>
<li>C++中使用inline关键字声明内联函数</li>
<li>内联函数声明时inline关键字必须和函数定义结合在一起，否则编译器会直接忽略内敛请求，这只是一种请求，编译器也可以拒绝</li>
</ul>
<pre><code class="language-c++">inline int func(int a, int b)
{
    return a &lt; b ? a : b
}
</code></pre>
<h2 id="内联函数特性">内联函数特性</h2>
<ul>
<li>C++编译器可以将一个函数进行内敛编译</li>
<li>被C++编译器内联编译的函数叫做内联函数</li>
<li>C++编译器直接将函数体插入函数调用的地方</li>
<li>内联函数没有普通函数调用时的额外开销（压栈、跳转、返回）</li>
<li><strong>C++编译器不一定满足函数的内敛请求</strong></li>
</ul>
<h2 id="内联函数初探">内联函数初探</h2>
<pre><code class="language-c++">#include &lt;stdio.h&gt;

#define     FUNC(a,b)   ((a) &lt; (b) ? (a) : (b))

inline int func(int a, int b)
{
    return a &lt; b ? a :b 
}

int main()
{
    int a = 1;
    int b = 3;
    int c = FUNC(++a, b);

    printf(&quot;a = %d\n&quot;, a); 
    printf(&quot;b = %d\n&quot;, b); 
    printf(&quot;c = %d\n&quot;, c);

    return 0; 
}
//如果调用FUNC(a, b)，打印3, 3, 3
//如果调用func(a, b)，   打印2, 3, 3
</code></pre>
<ul>
<li>内联函数具有普通函数的特征（参数检查，返回类型）</li>
<li>函数的内联请求可能被编译器拒绝</li>
<li>函数被内联编译后，函数体直接扩展到调用的地方</li>
<li>宏代码片段由预处理处理，进行简单的文本替换，没有任何编译过程，因此可能出现副作用</li>
</ul>
<h2 id="内联请求">内联请求</h2>
<ul>
<li>现代C++编译器能够进行编译优化，一些函数即使没有inline声明，也可能被内联编译</li>
<li>一些现代C++编译器提供了扩展语法，能够对函数进行强制内联，如：</li>
</ul>
<pre><code>    g++：__attribute__((always_inline))属性
    MSVC：__forceinline
</code></pre>
<h3 id="c中inline内联编译的限制">C++中inline内联编译的限制</h3>
<ul>
<li>不能存在任何形式的循环语句</li>
<li>不能存在过多的条件判断语句</li>
<li>函数体不能过于庞大</li>
<li>不能对函数进行取址操作</li>
<li>函数内联声明必须在调用语句之前</li>
</ul>
<p>#小结<br>
<strong>内联函数的提出是为了替代宏代码块的，原因是高效</strong></p>
<ul>
<li>C++中可以通过inline声明内联函数</li>
<li>编译器直接将内联函数体扩展到函数调用的地方</li>
<li>inline只是一种请求，编译器不一定允许这种请求</li>
<li>内联函数省去了函数调用时的压栈、跳转和返回的开销</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（5）：引用本质分析]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-5yin-yong-ben-zhi-fen-xi</id>
        <link href="https://leichen2552.github.io/post/hui-gu-5yin-yong-ben-zhi-fen-xi">
        </link>
        <updated>2020-01-30T01:49:37.000Z</updated>
        <content type="html"><![CDATA[<h1 id="c中的引用">C++中的引用</h1>
<h2 id="引用的意义">引用的意义</h2>
<ul>
<li>引用作为变量别名而存在，因此在一些场合可以代替指针</li>
<li>引用相对于指针来说具有更好的可读性和实用性</li>
</ul>
<h3 id="exampleswap函数的实现对比">Example：Swap函数的实现对比</h3>
<pre><code class="language-c++">void Swap(int&amp; a, int&amp; b)
{
    int t = a;
    a = b;
    b = t;
}

void Swap(int* a, int* b)
{
    int t = *a;
    *a = *b;
    *b = t;
}
</code></pre>
<p>这两种就好像手动挡的车与自动挡的车，自动挡的车好开，这就好比是引用</p>
<p>注意：</p>
<ul>
<li>函数的引用形参不需要进行初始化，它的初始化发生在函数调用的时候</li>
</ul>
<h2 id="特殊的引用">特殊的引用</h2>
<h3 id="const引用">const引用</h3>
<ul>
<li>在C++中可以声明const引用</li>
<li>语法：const Type&amp; name = var;</li>
<li>const 引用让变量拥有只读属性</li>
</ul>
<pre><code class="language-c++">int a = 4;
const int&amp; b = a;
int* p = (int*)&amp;b;

b = 5;              // Error，只读变量
*p = 5;             //OK，修改变量 a 的值
</code></pre>
<ul>
<li>现在已经知道引用是另一个变量的别名，因此一个引用绝对不可能是常量值的别名。换句话说，就是不能定义一个引用然后用常量值来初始化，这里的常量是字面常量，因为从概念上来说是不可行的。但是有一个例外，就是const引用，我么可以用字面常量来对一个const引用初始化，编译器这个时候就会真真正正产生一个只读变量。</li>
</ul>
<pre><code class="language-c++">const int&amp; b = 1;   //ok
int* p = (int*)&amp;b;

b = 5;                  //Error，只读变量

*p = 5;                 //OK，修改变量 b 的值
</code></pre>
<ul>
<li>当使用常量对const引用进行初始化时，C++编译器会为这个常量分配空间，并将引用名作为这段空间的别名</li>
<li>结论：使用常量对const引用初始化后将生成一个只读变量</li>
</ul>
<h2 id="引用有自己的存储空间吗">引用有自己的存储空间吗</h2>
<p>小测试：</p>
<pre><code class="language-c++">#include &lt;stdio.h&gt;
struct TRef
{
    char&amp; r;
};

int main()
{
    char c = 'c';
    char&amp; rc = c;

    TRef ref = {c};

    printf(&quot;sizeof(char&amp;) = %d\n&quot;,sizeof(char&amp;));   //1
    printf(&quot;sizeof(rc) = %d\n&quot;,sizeof(rc));                 //1

    printf(&quot;sizeof(TRef) = %d\n&quot;,sizeof(TRef));         //4
    printf(&quot;sizeof(ref.r) = %d\n&quot;,sizeof(ref.r));           //1

    return 0;
}
</code></pre>
<h2 id="引用的本质">引用的本质</h2>
<h3 id="引用在c内部实现是一个指针常量">引用在C++内部实现是一个指针常量</h3>
<p>Type&amp; name      <span class="katex"><span class="katex-mathml"><math><semantics><mrow><mo>←</mo><mo>→</mo></mrow><annotation encoding="application/x-tex">\leftarrow \rightarrow</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.36687em;vertical-align:0em;"></span><span class="mrel">←</span></span><span class="base"><span class="strut" style="height:0.36687em;vertical-align:0em;"></span><span class="mrel">→</span></span></span></span>        Type* const name</p>
<pre><code class="language-c++">void f(int&amp; a)              void f(int* const a)
{                           {
    a = 5;                      *a = 5
}                           }
</code></pre>
<p>注意：</p>
<ul>
<li>C++编译器在编译过程中用指针常量作为引用的内部实现，因此引用所占的空间大小与指针相同</li>
<li>从使用的角度，引用只是一个别名，C++为了实用性而隐藏了引用的存储空间这一细节</li>
</ul>
<h2 id="引用的意义-2">引用的意义</h2>
<ul>
<li>C++中的引用旨在大多数情况下代替指针</li>
<li>功能性：可以满足多数需要使用指针的场合</li>
<li>安全性：可以避开由于指针操作不当而带来的内存错误</li>
<li>操作性：简单易用，又不失功能强大</li>
</ul>
<h2 id="实例解读">实例解读</h2>
<ol>
<li>不能返回局部变量的引用</li>
</ol>
<pre><code class="language-c++">int&amp; demo()     //int* const
{
    int d = 0;

    printf(&quot;demo : d = %d\n&quot;, d);

    return d;       //return &amp;d，返回一个局部变量的地址就是有问题
} 
</code></pre>
<ol start="2">
<li>可以返回在全局存储区内变量的引用</li>
</ol>
<pre><code class="language-c++">int&amp; func()     //int* const
{
   static int s = 0;

   printf(&quot;func : s = %d\n&quot;, s);

   return s;   //return &amp;s，静态的局部变量的存储空间是一个全局的存储区，
               //所以说它的空间不会以为函数调用而摧毁，由于引用的本质是指针，
               //所以返回一个全局存储区中的地址是完全可以的
} 
</code></pre>
<p>#小结</p>
<ul>
<li>引用作为别名而存在旨在代替指针</li>
<li>const引用可以使得变量具有只读属性</li>
<li>引用在一起内部使用<strong>指针常量实现</strong></li>
<li>引用的最终本质为指针</li>
<li>引用尽可能的避开内存错误</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（4）：布尔类型和引用]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-4bu-er-lei-xing-he-yin-yong</id>
        <link href="https://leichen2552.github.io/post/hui-gu-4bu-er-lei-xing-he-yin-yong">
        </link>
        <updated>2020-01-29T03:24:00.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#c%E8%AF%AD%E8%A8%80%E4%B8%AD%E7%9A%84%E5%B8%83%E5%B0%94%E7%B1%BB%E5%9E%8B">C语言中的布尔类型</a></li>
<li><a href="#c%E4%B8%AD%E7%9A%84%E5%B8%83%E5%B0%94%E7%B1%BB%E5%9E%8B">C++中的布尔类型</a>
<ul>
<li><a href="#%E5%B8%83%E5%B0%94%E7%B1%BB%E5%9E%8B%E7%9A%84%E5%80%BC">布尔类型的值</a></li>
<li><a href="#%E5%B8%83%E5%B0%94%E7%B1%BB%E5%9E%8B">布尔类型</a>
<ul>
<li><a href="#%E5%B8%83%E5%B0%94%E7%B1%BB%E5%9E%8B%E6%98%AFc%E4%B8%AD%E7%9A%84%E5%9F%BA%E6%9C%AC%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B">布尔类型是C++中的基本数据类型</a></li>
</ul>
</li>
<li><a href="#%E4%B8%89%E7%9B%AE%E8%BF%90%E7%AE%97%E7%AC%A6">三目运算符</a>
<ul>
<li><a href="#c%E5%AF%B9%E4%B8%89%E7%9B%AE%E8%BF%90%E7%AE%97%E7%AC%A6%E8%BF%9B%E8%A1%8C%E4%BA%86%E5%8D%87%E7%BA%A7">C++对三目运算符进行了升级</a></li>
<li><a href="#c%E8%AF%AD%E8%A8%80%E4%B8%AD%E7%9A%84%E4%B8%89%E7%9B%AE%E8%BF%90%E7%AE%97%E7%AC%A6%E8%BF%94%E5%9B%9E%E7%9A%84%E6%98%AF%E5%8F%98%E9%87%8F%E5%80%BC">C语言中的三目运算符返回的是变量值</a></li>
<li><a href="#c%E4%B8%AD%E7%9A%84%E4%B8%89%E7%9B%AE%E8%BF%90%E7%AE%97%E7%AC%A6%E8%BF%94%E5%9B%9E%E7%9A%84%E6%98%AF%E5%8F%98%E9%87%8F%E6%9C%AC%E8%BA%AB%E6%B3%A8%E6%84%8F%E6%98%AF%E5%8F%98%E9%87%8F%E6%9C%AC%E8%BA%AB">C++中的三目运算符返回的是变量本身（注意是变量本身）</a></li>
</ul>
</li>
<li><a href="#c%E4%B8%AD%E7%9A%84%E5%BC%95%E7%94%A8">C++中的引用</a>
<ul>
<li><a href="#%E5%8F%98%E9%87%8F%E5%90%8D%E5%9B%9E%E9%A1%BE">变量名回顾</a></li>
<li><a href="#c%E5%AF%B9%E4%B8%89%E7%9B%AE%E8%BF%90%E7%AE%97%E7%AC%A6%E5%81%9A%E4%BA%86%E4%BB%80%E4%B9%88">C++对三目运算符做了什么</a></li>
</ul>
</li>
</ul>
</li>
<li><a href="#%E5%B0%8F%E7%BB%93">小结</a></li>
</ul>
</p>
<h1 id="c语言中的布尔类型">C语言中的布尔类型</h1>
<p>C语言中没有布尔类型</p>
<h1 id="c中的布尔类型">C++中的布尔类型</h1>
<ul>
<li>C++在C语言的基础类型系统里增加了bool</li>
<li>C++中的bool可取的值只有true和false</li>
<li>理论上bool只占用了一个字节</li>
</ul>
<p>注意：</p>
<ul>
<li>true代表真值，编译器内部用1来表示</li>
<li>false代表非真值，编译器内部用0来表示</li>
</ul>
<p>C++的两个加号，一个加号就是类型上面的增强，另一个加号就是面向对象的加强<br>
所以C语言中没有的布尔类型，在C++中就提供了这种bool类型</p>
<h2 id="布尔类型的值">布尔类型的值</h2>
<ul>
<li>bool类型只有true（非0）和false（0）两个值</li>
<li>C++编译器会将非0值转换为true，0值转换为false</li>
</ul>
<pre><code class="language-c++">//下面的代码会输出什么？
bool b = 0;
printf(&quot;b = %d&quot;,b);//0

b++;
printf(&quot;b = %d&quot;,b);//1

b = b - 3;
printf(&quot;b = %d&quot;,b);//1
</code></pre>
<p>但是C++中的布尔类型的内部实现还是用一个字节(byte)的整型来实现的。</p>
<pre><code class="language-c++">#include &lt;stdio.h&gt;

int main()
{
    bool b = false;
    int a = b;

    printf(&quot;sizeof(b) = %d\n&quot;, sizeof(b));//1
    printf(&quot;b = %d, a = %d&quot;, b, a);//0,0

    b = 3;
    a = b;

    printf(&quot;b = %d, a = %d&quot;, b, a);//1,1

    b = -5;
    a = b;

    printf(&quot;b = %d, a = %d&quot;, b, a);//1,1

    a = 10;
    b = a;

    printf(&quot;a = %d, b = %d&quot;, a, b);//10,1

    a = 0;
    b = a;

    printf(&quot;a = %d, b = %d&quot;, a, b);//0,0

    return 0;
}
</code></pre>
<h2 id="布尔类型">布尔类型</h2>
<h3 id="布尔类型是c中的基本数据类型">布尔类型是C++中的基本数据类型</h3>
<ul>
<li>可以定义bool类型的全局变量</li>
<li>可以定义bool类型的常量</li>
<li>可以定义bool类型的指针</li>
<li>可以定义bool类型的数组</li>
<li>其他基本类型能干的事情，bool类型都能干</li>
</ul>
<h2 id="三目运算符">三目运算符</h2>
<h3 id="c对三目运算符进行了升级">C++对三目运算符进行了升级</h3>
<pre><code class="language-c++">#include &lt;stdio.h&gt;

int main()
{
    int a = 1;
    int b = 2;

    (a &lt; b ? a : b) = 3;//等价于a = 3;

    printf(&quot;a = %d, b = %d\n&quot;, a, b);
}
</code></pre>
<h3 id="c语言中的三目运算符返回的是变量值">C语言中的三目运算符返回的是变量值</h3>
<ul>
<li>不能作为左值使用</li>
</ul>
<h3 id="c中的三目运算符返回的是变量本身注意是变量本身">C++中的三目运算符返回的是变量本身（注意是变量本身）</h3>
<ul>
<li>既可以作为右值使用，又可以作为左值使用</li>
</ul>
<p>牢记：</p>
<ul>
<li>三目运算符就只有在所有的可能返回都是变量的时候才能作为左值使用</li>
<li>如果混搭，编译器就会报错，像如下：</li>
</ul>
<pre><code class="language-c++">    int a = 1;
    int b = 2;

    (a &lt; b ? a : 2) = 3;//这句就会报错
</code></pre>
<p>注意：</p>
<ul>
<li>三目运算符可能返回的值中如果有一个常量值，则不能作为左值使用</li>
</ul>
<h2 id="c中的引用">C++中的引用</h2>
<h3 id="变量名回顾">变量名回顾</h3>
<ul>
<li>变量是一段实际连续存储空间的别名</li>
<li>程序是通过变量来申请并命名存储空间</li>
<li>通过变量的名字可以使用存储空间</li>
</ul>
<p>问题：一段连续的存储空间只能有一个别名吗？答案：不一定</p>
<p>###在C++中新增加了引用的概念</p>
<ul>
<li>引用可以看做是一个已定义变量的别名</li>
<li>引用的语法：Type&amp; name = var;</li>
</ul>
<pre><code class="language-c++">    int a = 4;
    int&amp; b = a;//b为a的别名

    b = 5; //操作b就是操作a
</code></pre>
<p>注意：</p>
<ul>
<li>普通引用在定义时必须用同类型的变量进行初始化。初始化一定是其他同类型（一定要同类型，不然编译器报错）变量的别名，不能用字面常量进行初始化（不然编译器报错）</li>
</ul>
<pre><code class="language-c++">#include &lt;stdio.h&gt;

int main()
{
    int a = 4;
    int&amp; b = a;

    printf(&quot;a = %d\n&quot;, a);
    printf(&quot;b = %d\n&quot;, b);//a、b值一样
    printf(&quot;&amp;a = %p\n&quot;, &amp;a);
    printf(&quot;&amp;b = %p\n&quot;, &amp;b);//a、b地址也一样
}
</code></pre>
<h3 id="c对三目运算符做了什么">C++对三目运算符做了什么</h3>
<ul>
<li>当三目运算符的可能返回都是变量时，返回的就是变量引用</li>
<li>当三目运算府的可能返回中有常量时，返回的是值</li>
</ul>
<pre><code class="language-c++">int a = 1;
int b = 2;

(a &lt; b ? a : b) = 3;//正确，返回 a 或 b 的引用，可作为左值
(a &lt; b ? a : b) = 4;//错误，返回 1 或 b 的值，不能作为左值 
</code></pre>
<h1 id="小结">小结</h1>
<ul>
<li>bool类型是C++新增加的基础类型</li>
<li>bool类型的值只能是true和false</li>
<li>C++中的三目运算符可作为左值使用</li>
<li>C++中的引用可以看做是变量的别名来使用</li>
<li>三目运算符的可能返回都是变量时，返回的是引用</li>
</ul>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[回顾（3）：进化后的const分析]]></title>
        <id>https://leichen2552.github.io/post/hui-gu-3jin-hua-hou-de-const-fen-xi</id>
        <link href="https://leichen2552.github.io/post/hui-gu-3jin-hua-hou-de-const-fen-xi">
        </link>
        <updated>2020-01-13T07:53:45.000Z</updated>
        <content type="html"><![CDATA[<p><ul class="markdownIt-TOC">
<li><a href="#%E8%BF%9B%E5%8C%96%E5%90%8E%E7%9A%84const%E5%88%86%E6%9E%90">进化后的const分析</a>
<ul>
<li><a href="#c%E8%AF%AD%E8%A8%80%E4%B8%AD%E7%9A%84const">C语言中的const</a></li>
<li><a href="#c%E8%AF%AD%E8%A8%80%E4%B8%AD%E7%9A%84const-2">C++语言中的const</a></li>
<li><a href="#c%E8%AF%AD%E8%A8%80%E4%B8%AD%E7%9A%84const%E5%8F%98%E9%87%8F">C语言中的const变量</a></li>
<li><a href="#c%E4%B8%AD%E7%9A%84const%E5%8F%98%E9%87%8F">C++中的const变量</a></li>
</ul>
</li>
<li><a href="#%E5%B0%8F%E7%BB%93">小结</a></li>
</ul>
</p>
<h1 id="进化后的const分析">进化后的const分析</h1>
<h2 id="c语言中的const">C语言中的const</h2>
<ul>
<li><font  face = "Courier New" color = blue size = 4><strong>const</strong></font>修饰的变量是只读的，本质还是变量</li>
<li><font  face = "Courier New" color = blue size = 4><strong>const</strong></font>修饰的局部变量在栈上分配空间</li>
<li><font  face = "Courier New" color = blue size = 4><strong>const</strong></font>修饰的全局变量在只读存储器分配空间</li>
<li><font  face = "Courier New" color = blue size = 4><strong>const</strong></font>只在编译器有用，在运行期无用</li>
<li><font  face = "Courier New" color = blue size = 4><strong>const</strong></font>修饰的变量不是真正的常量，它只告诉编译器该变量不能出现在赋值符号的左边</li>
<li>C语言中的<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>使得变量具有只读属性</li>
<li><font  face = "Courier New" color = blue size = 4><strong>const</strong></font>将具有全局生命周期的变量存储于只读存储区</li>
<li><font  face = "Courier New" color = blue size = 4><strong>const</strong></font>不能定义真正意义上面的常量！</li>
</ul>
<p><font color = ff0000>C语言上定义<strong>真正意义上的常量</strong>只能用<font color = 007f00 size = 5><strong>枚举</strong></font></font></p>
<h2 id="c语言中的const-2">C++语言中的const</h2>
<ul>
<li>C++在C的基础上对<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>进行了进化处理
<ul>
<li>当碰见<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>声明时在符号表中放入常量</li>
<li>编译过程中若发现使用常量则直接以符号表中的值替换</li>
<li>编译过程中若发现下述情况则给对应的常量分配存储空间
<ol>
<li>对<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>常量使用了extern</li>
<li>对<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>常量使用&amp;操作符<br>
C++编译器虽然可能为<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>分配空间，但不会使用其存储空间的值</li>
</ol>
</li>
</ul>
</li>
</ul>
<h2 id="c语言中的const变量">C语言中的const变量</h2>
<ul>
<li>C语言中的<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>变量是<font color = 007fff size = 3>只读<font color = green size = 4>变量</font></font>，本质仍然是变量，会分配存储空间</li>
</ul>
<h2 id="c中的const变量">C++中的const变量</h2>
<ul>
<li>可能会分配存储空间。当<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>常量为全局，并且需要其他文件中使用。当使用 &amp; 操作符对const常量取地址</li>
<li>C++中的<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>常量类似于宏定义。const int c = 5;<span class="katex"><span class="katex-mathml"><math><semantics><mrow><mo>≈</mo></mrow><annotation encoding="application/x-tex">\approx</annotation></semantics></math></span><span class="katex-html" aria-hidden="true"><span class="base"><span class="strut" style="height:0.48312em;vertical-align:0em;"></span><span class="mrel">≈</span></span></span></span>#define   c   5</li>
<li>C++中的<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>常量在与宏定义不同。<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>常量是由编译器处理，编译器对<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>常量进行类型检查和作用域检查</li>
<li>宏定义由预处理器处理，单纯的文本替换</li>
</ul>
<h1 id="小结">小结</h1>
<ul>
<li>与C语言不同，C++中的<font  face = "Courier New" color = blue size = 4><strong>const</strong></font><font color = 7f0000>不是只读变量</font>，而C语言中const只带了只读属性，本质上还是一个变量。</li>
<li>C++中的<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>是一个<font color = purple><strong>真正意义上的常量</strong></font></li>
<li>C++编译器<font size = 4><strong>可能</strong></font>会为<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>常量分配空间</li>
<li>C++<font color = green>完全兼容</font>C语言<font  face = "Courier New" color = blue size = 4><strong>const</strong></font>常量的<font color = green>语法特性</font></li>
</ul>
]]></content>
    </entry>
</feed>